Original prompt: the music for this game is currently horrible and very hard to listen to, please work it to be something that players will want to continue listening to as they play

Killer co-location delay follow-up (current prompt): murders during play now require the selected victim and killer to remain in the same room for five uninterrupted real seconds. Each killer-victim timer resets when they separate, and all timers clear after a murder/teleport. The pre-game opening crime remains exempt.
- Verification: production build, focused ESLint, full simulation smoke test, and diff check pass. Focused assertions cover 4.99-second denial, 5.00-second permission, detective/crowd restrictions, and leave/re-enter reset behavior. The mandated client was run, and a targeted live-case capture in `output/killer-colocation-delay/` was visually inspected with zero console/page errors.

Groq HTTP 413 interview-plan correction (current prompt): replaced the single 7,000-token, four-route chat completion with two sequential two-route requests capped at 3,200 tokens each. The complete immutable four-route plan and strict validation remain unchanged, while each provider request stays below the size that the default Groq connection was rejecting.
- Added `llm-plan-test.ts`, which mocks the OpenAI-compatible endpoint and verifies four validated routes are assembled from exactly two requests using the 3,200-token cap.
- Verification: focused mocked-plan test, production build, focused ESLint, and diff check pass. The mandated web-game browser client completed in `output/llm-413-fix-runtime/`; its title capture was visually inspected and no browser error artifact was produced.

Conversation camera follow-up (current prompt): when an interview begins, the camera now pans to the midpoint between the detective and interviewed guest so both actors share the center of the frame. Ending the interview restores normal player/room camera tracking.
- Verification: production build, focused game/world ESLint, and diff check pass. The mandated web-game client completed in `output/conversation-camera-runtime/`. Targeted interview coverage in `output/conversation-camera-targeted/` confirms the desired camera focus equals the exact player/guest midpoint, the pan settles there, and no browser errors occur; the final interview screenshot was visually inspected.
- Composition refinement: conversation focus is shifted 0.75 world units left of the actor midpoint, placing the detective and guest slightly right of screen center while preserving their shared framing.
- Composition verification: production build, focused world ESLint, diff check, and mandated browser client pass. Targeted runtime metadata confirms the exact 0.75-unit offset with zero browser errors; `output/conversation-camera-right-targeted/interview.png` was visually inspected and shows the conversation pair shifted right with comfortable HUD clearance.

NPC walking-animation follow-up (current prompt): walking animation now requires actual position displacement in addition to the simulation's walk intent. NPCs frozen for detective interviews, paused mid-route, or otherwise not physically traveling render their idle pose instead of walking in place.
- Verification: production build, focused world ESLint, and diff check pass. The mandated browser client completed without a reported browser error; its setup capture was visually inspected in `output/npc-walk-animation/`.

LLM response fallback follow-up (current prompt): selected LLM games appeared to always return built-in responses.
- Root cause: the game requests a complete four-route/two-stage conversation plan, but capped the response at 3,200 tokens. Truncated JSON fails strict validation and silently falls back to authored dialogue.
- Increased the plan timeout/output allowance to 45 seconds/7,000 tokens and added an explicit interview source label (`LLM dialogue`, `Built-in dialogue`, or `LLM failed · built-in fallback`) so provider/configuration failures can no longer masquerade as successful LLM output.
- Production build and focused ESLint pass. The mandated runtime client completed in `output/llm-fallback-runtime/`. Mocked provider coverage confirmed the request now sends `max_tokens: 7000`, a valid generated plan renders four model-authored questions under `LLM dialogue`, and an invalid/truncated response visibly reports `LLM failed · built-in fallback`; both states were visually inspected in `output/llm-fallback-targeted/` with zero browser errors.

Procedural furnishing removal follow-up (current prompt): removed the remaining legacy 3D furnishing builder and its call from every room, including tables, chairs, shelves, beds, cabinets, counters, exhibits, plants, barrels, and the old ballroom chandelier. Removed the matching furnishing metadata and invisible collision footprints so cleared floor space is fully walkable. Floors, walls, doors/passages, storm windows, and practical room lighting remain in place for the upcoming sprite replacement pass.
- Verification: production build, focused ESLint, and diff check pass. The mandated web-game client reached a live case and moved the detective through the former Dining Hall table footprint. The gameplay screenshot was visually inspected at `output/furnishings-removed-gameplay/shot-0.png`; its text state reports the detective at x=-2.79 inside the old table collider, and no browser error artifact was produced.

Room wall sprite rework follow-up (current prompt): replaced flat color-box walls with nine deterministic pixel-art CanvasTexture treatments, one authored material language per room. Exterior-wall windows now use reusable storm-window assemblies with dark rain, preflash, flash, and afterglow states, animated rain streaks, lightning-vein art, and localized flash light. The cellar now has two iron wall sconces with deterministic flame movement and flickering point lights.
- Rain containment correction: every animated streak endpoint is clamped to the glass rectangle in both axes, preventing lines from bleeding over wall surfaces while wrapping or swaying.
- Verification: production build, focused ESLint, and diff check pass. The mandated browser client completed gameplay and title-screen captures; the final wall-palette and rain-clipping screenshots were visually inspected in `output/wall-sprites-brightness-final/` and `output/window-rain-clipped/`.

Hallway sprite rework follow-up: added four coordinated passage families—formal walnut, service tile, public stone, and damp flagstone—with connection-specific floor masters for all twelve mansion links and matching textured side-wall/trim assemblies. Mapping, orientation, mirroring, and room-color transition bands are deterministic.
- Hallway junction correction: room door openings are 4.2 units wide while passage lanes are 3 units wide, so every hallway end now uses short perpendicular return wings to close the visible side notches while preserving the full walkable lane.
- Verification: production build, focused hallway/world ESLint, and diff check pass. Browser captures were visually inspected in `output/hallway-sprites-runtime/` and `output/hallway-gap-fix/`.
- Entrance crown-moulding follow-up: every hallway end now has a layered lintel and cap themed to its passage family. Formal walnut uses repeated dentils, service corridors use restrained tile keys, public stone uses a central keystone with side blocks, and damp passages use irregular rough-stone drops. All pieces remain at the wall line above the walking clearance.
- Crown verification: production build, focused hallway-wall ESLint, and diff check pass. The gameplay capture in `output/hallway-crown-moulding/` was visually inspected and reports no browser error artifact.
- High-quality hallway floor follow-up: replaced the old 160×128 code-painted floor canvases with four authored 1254×1254 raster sprites generated specifically as orthographic, edge-to-edge game textures: walnut runner, service tile, formal stone, and damp flagstone. The loader now uses sRGB color, linear magnification, mipmapped minification, anisotropic filtering, UV clamping, and connection-specific mirroring to eliminate blockiness, edge bleeding, and stretched micro-pattern artifacts.
- HQ floor verification: production build, focused hallway/world ESLint, and diff check pass. Both title and unobstructed gameplay captures were visually inspected in `output/hallway-hq-sprites/` and `output/hallway-hq-sprites-gameplay/`; no browser error artifact was produced.
- Hallway threshold cleanup: removed the separate raised brass sill meshes from every room doorway so the room and hallway floor sprites meet directly without the distracting crosswise bar.
- Threshold verification: production build, focused world ESLint, and diff check pass. The gameplay capture in `output/hallway-no-thresholds/` was visually inspected; joins remain covered and no browser error artifact was produced.

Room lighting pass (current prompt): replaced the shared single-bulb treatment with two authored practical pools per room, positioned and tuned to each room's function and mood. All perimeter rooms now receive windows on their true exterior walls (corner rooms receive two), with localized blue-white window light during intermittent lightning; global moon/ambient lift is restrained and reliably resets after each flash. Body-room lighting still recolors every practical in the affected room.
- Verification: `git diff --check`, production build, and focused world ESLint pass. The mandated web-game runtime completed in `output/lighting-pass-runtime/`. Normal captures for representative themed rooms plus normal/forced-flash Master Suite comparison are in `output/lighting-pass-targeted/`; the captures were visually inspected and metadata reports zero browser errors.

Ceiling-light sprite refinement (current prompt): narrowed the detailed fixture work to hanging ceiling lights only, leaving room furnishings unchanged for their planned rework. Generated and chroma-keyed two transparent period pixel-art pendants under `public/assets/lighting/`: an emerald/brass Library fixture and a rose-glass Art Deco Master Suite fixture. Both replace the generic visible bulbs in those rooms and drive broader, higher practical-light pools.
- Raised neutral ambient fill, extended practical-light reach, softened fog, and fixed light reset to restore each authored fixture's actual intensity. Wall paneling, shelving, trim, and floor detail remain readable between lightning flashes.
- Verification: production build, focused world ESLint, diff check, mandated web-game runtime, and targeted Library/Suite captures pass. Final screenshots in `output/ceiling-lighting-final/` were visually inspected; metadata reports zero browser errors.

All-room ceiling fixture follow-up (current prompt): completed a cohesive hanging-light sprite family for every room without changing furnishings. Added a shaded brass Study pendant, formal twin Gallery pendant, two verdigris Conservatory cage lights, two simple Kitchen bulbs, a broad Dining candelabra, a grand tiered crystal Ballroom chandelier, two rugged Cellar cage bulbs, plus the existing Library and Master Suite pendants. Practical-light positions, reach, and intensity now follow those ceiling fixtures.
- Verification: production build, focused world ESLint, and diff checks pass. A settled-camera browser sweep captured all nine rooms and was visually reviewed as `output/all-ceiling-lighting-final-settled/contact-sheet.jpg`; its metadata reports zero console/page errors.

Ceiling fixture alignment/cutout correction (current prompt): redrew the entire nine-fixture sheet as strict front-elevation pixel art with vertical center axes, level crossbars, and no three-quarter perspective. Replaced all nine transparent runtime assets in place. Removed the legacy fully transparent bulb sphere, whose default depth write was punching a circular hole through the visible sprite at the light source.
- Verification: production build, focused world ESLint, diff checks, and the mandated browser client pass. Targeted Kitchen bulb, Ballroom chandelier, and Library pendant captures in `output/ceiling-lighting-alignment-fix/` were visually inspected: fixtures hang straight, bulbs remain filled, and the circular cutout is gone; metadata reports zero browser errors.

Ceiling insect effects (current prompt): added fixture-local insect swarms without changing room furnishings. Each Cellar cage bulb attracts seven tiny dark flies with fast, jittering elliptical paths; each Conservatory pendant attracts five pale triangular moths with slower looping paths and animated wing-width flutter. All movement is driven by the world time step for deterministic browser testing.
- Verification: production build, focused world ESLint, diff checks, and the mandated browser client pass. Targeted before/after captures for both rooms were visually inspected in `output/ceiling-insects-final/`; runtime metadata confirms 14 flies, 10 moths, motion in both samples, and zero console/page errors.

Cellar window sconce balance (current prompt): mirrored the existing west-wall iron flame sconce to the opposite side of the exterior window, using the same height, wall offset, materials, flame animation, and glow with a separate flicker phase. The existing south-wall sconce remains unchanged.
- Verification: production build, focused world/fixture ESLint, diff check, and mandated browser client pass. The settled Cellar capture at `output/cellar-window-sconce-balance/cellar.png` was visually inspected; the west window is evenly bracketed and metadata reports zero browser errors.

Pre-round AI mode choice follow-up (current prompt): added a dedicated case setup phase before a new or restarted round. The player must explicitly choose Built-in Game or LLM Game, sees a warning when LLM mode has no configured connection, and confirms the selected mode before the simulation is created.
- Verification: production build and focused ESLint pass. The mandated web-game client reached the new `setup` phase in `output/pre-round-mode-runtime/`; the selector screenshot was visually inspected and its state reports the correct pre-game phase.
- LLM setup refinement: selecting LLM Game now expands provider, base URL, API key, and model controls directly on the pre-round screen. Hosted/custom LLM cases cannot start until an API key is supplied; Ollama remains available without a key and shows its local-runtime requirement.
- LLM setup verification: production build and focused Screens ESLint pass. The mandated browser client selected LLM Game and captured the complete provider/key/model panel in `output/pre-round-llm-config-runtime/`; the screenshot was visually inspected and the state remained correctly in the pre-game `setup` phase.

Sprite room decor removal follow-up (current prompt): removed the sprite-decor import, texture cache, loader, builder, and mansion build call from `src/game/world.ts`. Generated sprite files, placement modules, and the layout design remain preserved but are no longer loaded or rendered, making restoration straightforward.

Hidden shuffled evidence + LLM reveal follow-up (current prompt): evidence profiles are now reassigned between guests with the seeded case RNG while preserving exactly three traces per guest and three owners per trace. All journal associations begin as question marks. Each successful LLM interview rolls a game-controlled 30% chance to receive one still-hidden association as a private cue; the prompt requires an indirect in-character hint without naming the evidence, plus a stripped control tag. The journal unlock occurs only when the response confirms the hint was incorporated. Scene-evidence candidate lists resolve known guest names dynamically and retain numbered unknown sources for unrevealed owners.
- Verification: production build, focused ESLint, and the full simulation smoke test pass. The mandated browser client started a live case without browser errors. Targeted mocked-LLM coverage forced the reveal roll, unlocked exactly one of the guest's three shuffled associations, stripped every control tag from visible dialogue, and produced the visually inspected mixed revealed/unknown journal state in `output/hidden-evidence-targeted/` with zero browser errors.
- Built-in parity follow-up: offline interviews now use the same per-answer 30% reveal roll. Every trace has authored indirect variations describing an ordinary smell, material, possession, stain, or habit without speaking the journal evidence label. Successful built-in hints unlock the matching shuffled slot and add the same evidence lead; configured-LLM failure fallbacks preserve the rolled clue instead of losing it.
- Built-in parity verification: production build, focused ESLint, and the full simulation smoke test pass, including authored-hint coverage for all ten evidence types. The mandated browser client started a clean case. Targeted built-in UI coverage forced a reveal from zero known associations, displayed a natural ink-related aside without calling it evidence, then unlocked exactly `ink-fiber` while leaving the other two slots unknown. Dialogue and journal screenshots were visually inspected in `output/builtin-evidence-targeted/`; metadata reports zero browser errors.
- Evidence discovery notification follow-up (current prompt): unlocking a hidden guest association now triggers the same mystery/body-discovery audio stinger and a dedicated 4.2-second top-center notification with the evidence sprite, evidence label, associated guest, and journal confirmation. The shared reveal method covers built-in answers, LLM answers, and built-in fallbacks; repeat associations remain guarded from duplicate notifications.
- Notification verification: production build and focused ESLint pass. The mandated browser client starts cleanly. Targeted built-in coverage recorded exactly one play of the existing `bodyDiscoveryCue`, confirmed the notice is visible at full opacity with foreground z-index 60, and confirmed automatic dismissal after 4.2 seconds with zero browser errors. The corrected final notification screenshot was visually inspected at `output/evidence-notification-targeted/notification-final.png`.

Post-murder teleport follow-up (current prompt): the killer now relocates immediately after every murder to a different room, never the detective's current room, instead of visibly walking out through a doorway. The arrival uses a walkable, unoccupied point and clears any prior movement or conversation state. Regression coverage verifies the killer leaves the scene, avoids the player, arrives idle, and retains no escape path.
- Verification: production build, focused ESLint, and the full simulation smoke test pass. The mandated browser client started and advanced a live case without browser errors; its matching state and visually inspected gameplay screenshot are in `output/killer-teleport-runtime/`.

Detective investigation scale follow-up (current prompt): the investigation action now holds the authored notebook pose for its full duration instead of swapping mid-action to the much wider kneeling atlas cell. The sprite root remains at a fixed 1:1 scale, preserving the detective's proportions and preventing the apparent grow/shrink pulse.
- Verification: production build and focused ESLint pass. The mandated web-game client completed in `output/detective-investigation-scale-runtime/`. A targeted deterministic action sampled the detective at 250 ms intervals: every active sample remained on frame 15 with sprite scale `{ x: 1, y: 1, z: 1 }`, then returned to the directional idle frame at the same scale. The mid-action screenshot was visually inspected and the run reported zero browser console errors.

LLM choice persistence follow-up (current prompt): keep the player's saved AI Director choice visibly active instead of making them appear to reactivate LLM mode.
- Settings already persist through `murder-mansion-settings` in localStorage. Corrected the HUD to display the saved `settings.director` choice; the transient `llmActive` health flag no longer makes a selected LLM appear to revert to built-in between successful requests.
- Production build and focused ESLint pass. The mandated web-game client completed in `output/llm-choice-persistence-runtime/`. A targeted reload test restored the saved Ollama/LLM choice, started a new case with `AI: LLM` still visible, and reported zero console/page errors; the screenshot was visually inspected in `output/llm-choice-persistence-targeted/`.

Interview time follow-up (current prompt): simulation time now continues during detective interviews while the interviewed NPC is movement- and decision-locked in place; other guests continue their normal activity. The synchronized clock audio also continues, and the How to Play copy no longer says interviews pause time.
- Removed the stale interview-panel “Time stands still” caption; it now reads “The night moves on.”
- Verification: production build and focused ESLint pass. The mandated web-game client completed in `output/interview-time-runtime/`. Targeted deterministic coverage in `output/interview-time-targeted-final/` advanced an interview from 12:00 to 12:05: the interviewed NPC's coordinates remained identical while another routed NPC moved across the mansion, the phase remained `interview`, and there were zero console/page errors. The final interview screenshot was visually inspected and shows the updated clock and “The night moves on” caption.

NPC entrance fade follow-up (current prompt): guests in a newly entered room now ease from transparent to fully visible over the first 2.8 world units past a valid doorway, using smoothstep opacity on the atlas, shadow, and name label so room transitions no longer make them pop into view.
- Verification: production build and focused ESLint pass. The mandated web-game client completed in `output/npc-entrance-fade-runtime/` without an error artifact. Targeted doorway captures in `output/npc-entrance-fade-targeted/` confirm a guest at 8.4% opacity just inside the entrance and 100% deeper in the room; both screenshots were visually inspected and metadata reports zero console/page errors.
- Exit-fade correction: room-entry opacity is now monotonic. It resets only when crossing into a new room, rises with entrance depth, and remains fully visible when the detective later approaches any exit.
- Exit-fade verification: production build and focused ESLint pass. The mandated client completed in `output/npc-exit-fade-runtime/`. Targeted enter/deepen/exit sampling in `output/npc-exit-fade-targeted/` recorded opacity `0.084 → 1 → 1`; the exit screenshot was visually inspected and metadata reports zero console/page errors.
- Bidirectional doorway fade correction: guests now fade back out while the detective approaches an exit. Once the detective crosses into the unassigned passage, outgoing opacity is latched downward so the old room's guests cannot fade back in before the next room activates.
- Bidirectional fade verification: production build and focused ESLint pass. The mandated client completed in `output/npc-bidirectional-fade-runtime/`. Targeted sampling in `output/npc-bidirectional-fade-targeted/` confirms opacity `1 → 0.292 → 0.0037 → 0.0037` from room center through the passage; both exit-transition screenshots were visually inspected and metadata reports zero browser errors.
- Hallway-threshold refinement: guests remain fully opaque throughout the room, including at the inside edge of a doorway. The exit fade now begins only after the detective crosses into the hallway and progresses over 2.2 world units, while retaining the passage latch that prevents reappearing.
- Hallway-threshold verification: production build and focused ESLint pass. The mandated client completed in `output/npc-hall-threshold-runtime/`. Targeted captures in `output/npc-hall-threshold-targeted/` confirm opacity remains `1` just inside and exactly on the threshold, then falls to `0.994`, `0.568`, and `0` through the hall; screenshots were visually inspected and metadata reports zero browser errors.

Controlled sprite decor follow-up (current prompt): decorate all nine rooms with the generated decor sprites using intentional compositions rather than gap-filling placement.
- A three-agent room team first exposed the weakness of independent placement: forced use of all 45 assets produced redundant furniture, inconsistent scale, and scattered floor props.
- Added `DECOR_LAYOUT_DESIGN.md` as the shared design authority. It defines physical scale classes, floor/tabletop/plinth/wall anchors, camera-depth rules, protected doorway lanes, exact coordinates, and a named composition for every room.
- The final layout uses 34 purposeful placements in one or two vignettes per room and explicitly omits 11 sprites that duplicate existing procedural beds, desks, chairs, bookcases, ranges, barrels, and other landmarks.
- Added typed placement modules under `src/game/decor/`, plus cached transparent sprite loading in `world.ts`. `baseY` supports tabletop, plinth, and wall attachment; aspect ratios are taken from the loaded source images.
- Verification: production build and focused ESLint pass. The mandated web-game client capture is in `output/decor-controlled-dining/`. Isolated captures of all nine rooms and a reviewed contact sheet are in `output/decor-controlled-rooms/`; runtime console/page error list is empty.

Room small-item cleanup follow-up (current prompt): remove all small room objects while retaining the main furniture so sprite props can replace them later.
- Removed procedural books, papers, desk accessories, dishes, candles, plants, sculptures, cookware, knives, cosmetics, and other small decorative geometry from all nine rooms.
- Retained the major furniture and fixtures: tables, chairs, shelves, beds, counters/appliances, piano, pedestals, benches, greenhouse structure/planter, chandelier, barrels, crates, wardrobes, vanity, mirror, ladder, and sideboards.
- Shelving now renders empty, ready for future sprite dressing. Removed the unused small-prop rendering helpers and material cache.
- Production build and focused world ESLint pass. The mandated live browser client traversed Dining Hall to Library; both cleaned rooms were visually inspected in `output/room-small-item-cleanup/`, retained furniture remains readable, movement/room transition works, and no browser error artifact was produced.

Pre-murder interview questions follow-up (current prompt): when no body has been discovered, interviews offer a distinct, less accusatory set of four questions about the evening, unease, social contact, and the current room. Once a victim is known, the existing investigative question pool returns.
- Verification: production build and focused ESLint pass. The mandated web-game client started a case without browser errors. Targeted captures in `output/pre-murder-questions-targeted/` verify all four pre-murder questions fit the dialog and that a discovered victim restores the investigative pool; both screenshots were visually inspected and metadata reports zero console/page errors.

Progressive interview questions follow-up (current prompt): interviews now advance through victim-count stages. The first known death adds victim/last-seen questions; the second adds connections; the third adds patterns and beneficiaries; four or more deaths replace the menu with selection, motive, survival, and next-target questions.
- Added built-in contextual answers and journal leads for last-seen, connection, motive, survival, and next-victim topics. LLM interviews now receive the exact on-screen question wording as well as full victim context.
- Verification: production build, focused ESLint, and the full-night simulation pass. The mandated browser client ran without errors. Targeted UI/answer coverage for one through four discovered victims is in `output/progressive-questions-targeted/`; all four screenshots were visually inspected, every menu fits cleanly, every new topic returns a matching answer, and metadata reports zero console/page errors.
- Built-in AI hardening follow-up: expanded the simulation regression suite to exercise all five progressive topics across every archetype, both last-seen/contact branches and their leads, and the single-survivor endgame where no other guest can be selected. Live built-in UI coverage asked all four late-game questions and verified contextual answers plus thoughtful, suspicious, worried, and surprised portrait states. Build, focused ESLint, simulation, and the mandated client pass; inspected artifacts are in `output/builtin-progressive-ai-targeted/` with zero browser errors.

Conversation emotion-tag follow-up (current prompt): LLM interview answers should select the conversation portrait expression with a trailing emotion tag that is removed before display.
- Built-in AI emotion follow-up: offline answers now select from the same six authored expressions using topic, killer status, alibi strength, archetype temperament, and repeated-question count. Suspicion/motive becomes suspicious, death/danger becomes worried, recall/pattern questions become thoughtful, next-victim questions become surprised, and repeated probing escalates from guarded to angry. LLM failure fallbacks use the same built-in selector.
- Built-in emotion verification: production build and focused ESLint pass. The mandated client completed in `output/builtin-emotions-runtime/`. A targeted offline-only interview with the chauffeur exercised all six states in one sequence—neutral, thoughtful, suspicious, worried, surprised, then angry after repeating a guarded topic. Metadata reports `director: builtin`, all six unique emotions, and zero browser errors; the final angry portrait screenshot was visually inspected in `output/builtin-emotions-targeted/`.
- Expression audit follow-up: visually reviewed the authored atlas slots and corrected the shared vocabulary to neutral, suspicious, worried, angry, thoughtful, and surprised. The former `happy` label was inaccurate for the visibly shocked/surprised sixth frame.
- Added archetype-specific reaction guidance for all ten NPCs plus explicit expression-selection rules. Interview prompts now include total prior questions and repeated-topic count, direct pressure/repetition toward anger or suspicion, and tell the model not to default to neutral when a more specific reaction fits.
- Expression-rule verification: production build and focused ESLint pass. The mandated runtime client completed in `output/conversation-expression-rules-runtime/`. A targeted mocked-LLM sequence asked the same pressure question twice; the second request contained the prior answer, `1` prior question, `1` prior use of the topic, all six choices, and the correspondent-specific anger rule. Its `[angry]` response rendered the angry atlas portrait with the tag removed and zero browser errors; screenshot and metadata were visually inspected in `output/conversation-expression-rules-targeted/`.
- Added a six-state conversation emotion protocol matching the six authored portrait frames: neutral, happy, suspicious, worried, angry, and thoughtful. Interview prompts require exactly one trailing tag, and the response parser strips it before answers or transcripts are stored.
- Interview state now carries the parsed emotion directly to the atlas portrait; thinking remains thoughtful, initial/built-in responses safely default to neutral, and missing/unknown tags also fall back to neutral.
- Verification: production build and focused ESLint pass. The mandated web-game client completed in `output/conversation-emotion-runtime/`. A targeted browser check parsed an `[angry]` response, rendered the angry atlas frame, confirmed the tag was absent from visible text, and reported zero browser errors; inspected artifacts are in `output/conversation-emotion-targeted/`.
- Parenthesized-tag compatibility follow-up: the parser now accepts either `[angry]` or `(angry)` for all six supported emotions, while leaving unrelated trailing parentheticals untouched. The prompt still explicitly requests square brackets.
- Parenthesized-tag verification: production build and focused ESLint pass. The mandated client completed in `output/conversation-parenthesis-runtime/`. Targeted live UI coverage confirmed `(angry)` is removed and selects the angry portrait, bracket tags still parse, `(after midnight)` remains dialogue, and no browser errors occur; inspected artifacts are in `output/conversation-parenthesis-targeted/`.
- Emotion-value residue follow-up: expanded suffix parsing to remove the complete control value for labeled forms such as `(emotion: angry)`, `(emotion) angry`, and `Emotion: angry`, plus a bare emotion when it appears alone on the final line. Ordinary prose and unrelated parentheticals remain untouched.
- Prompt-format clarification follow-up: interview requests now define the exact `<spoken dialogue> [emotion]` schema, enumerate the six valid final tokens, provide a valid example, and explicitly forbid parentheses, labels, JSON, Markdown, stage directions, newlines before the tag, and trailing text.
- Prompt-format verification: production build and focused ESLint pass. The mandated live browser client completed in `output/conversation-format-prompt-runtime/`; its gameplay screenshot was visually inspected and no browser error artifact was produced.

NPC atlas expansion (current prompt): generate movement atlases for all ten NPCs, plus face-down deceased poses with mild blood, matching chalk outlines, and multiple dialogue face sprites; integrate every state.
- Standardized each NPC atlas as a 4x5 grid: idle directions, two four-direction walking rows, deceased, chalk outline, and six dialogue expressions.
- Runtime integration now loads `<archetype>-atlas.png`, selects directional idle/walk frames, places deceased art flat on the floor, swaps investigated bodies to the generated chalk frame, and crops mood-varying dialogue faces from the atlas.
- Focused ESLint and TypeScript compilation pass while parallel asset generation is underway.
- Completed all ten final transparent atlases plus retained chroma masters. Every final is content-aware repacked into a strict 4x5 grid of square cells with transparent seams, so no pose is cut or stretched during sampling.
- Final contact sheet `output/npc-atlas-qa-final.png` was visually inspected across every identity's idle, deceased, chalk, and dialogue frames.
- Targeted runtime state verification in `output/npc-atlas-states/` covers a living vocalist, face-down deceased floor pose, generated chalk replacement, and accountant dialogue face; metadata reports zero browser errors. The mandated web-game client also completed at `output/npc-atlas-runtime/` without error artifacts.
- `npm run build`, focused ESLint, and TypeScript compilation pass.

Sprite scale normalization follow-up (current prompt): synchronize character scale across identities/poses and standardize conversation bust framing.
- Mechanically normalized all eleven final atlases without regenerating artwork. Standing, walking, and directional cells now occupy a consistent 86% of cell height with common foot alignment; floor poses/outlines share a common footprint envelope.
- Conversation frames now fill either 84% cell width or 86% height, are horizontally centered, and share the same bottom-aligned bust framing.
- Removed an inherited detached chalk fragment from the magician's neutral dialogue cell. Pre-normalization finals are retained in `output/sprite-scale-before/` for recovery.
- Final atlas QA contact sheet: `output/sprite-scale-normalized-qa.png`. In-game ten-NPC lineup and dialogue capture: `output/sprite-scale-lineup/`; metadata reports zero browser errors. Mandated web-game client also passes in `output/sprite-scale-runtime/`.
- `npm run build` and focused ESLint pass.

Character sway removal follow-up (current prompt): removed the sprite-root Z-axis rocking from both walking and idle animation paths. Directional frame animation, subtle vertical gait motion, and idle breathing remain; every living character now stays laterally upright.
- `npm run build` and focused ESLint pass. The mandated browser run exercised idle, movement, and return-to-idle states; `output/no-character-sway/shot-0.png` was visually inspected and no browser error artifact was produced.
- Residual-sway follow-up: removed all remaining procedural whole-sprite transforms—idle breathing scale, walking compression/stretch, and vertical bob. Authored atlas frame changes and direction mirroring are now the only visible sprite animation.
- `npm run build` and focused ESLint pass. The mandated client exercised left/right travel and idle recovery in `output/no-character-transform/`; the gameplay screenshot was visually inspected, characters remain rigidly upright, and no browser error artifact was produced.
- Detective heading follow-up: removed smooth heading interpolation for the player sprite. The detective now snaps its hidden heading to the requested direction and immediately selects the corresponding atlas frame, preventing slow sideward drift during movement.
- `npm run build` and focused ESLint pass. The mandated client exercised repeated left/right reversals in `output/detective-heading-snap/`; the final gameplay capture was visually inspected and no browser error artifact was produced.
- Detective authored-tilt follow-up: atlas inspection showed the remaining lean is baked into lateral walk cells 9 and 11. Sideways detective travel now holds the upright authored walk cells 5 and 7; front/back walking retains its two-frame gait.
- Final lateral-tilt correction: runtime captures showed even side-walk cells 5 and 7 read as leaning because of their extended-leg stance. Lateral detective movement now uses the fully upright profile cells 1 and 3; front/back movement retains authored gait animation.
- `npm run build` and focused ESLint pass. The mandated client exercised repeated full-width left/right travel in `output/detective-no-side-tilt/`; the final upright profile capture was visually inspected and no browser error artifact was produced.
- Side-gait restoration: lateral movement now alternates correctly paired poses despite the atlas's opposite idle/walk column conventions—right uses frames 3/5 and left uses 1/7. This restores animation while preserving correct facing and avoiding the more strongly leaning second-step frames 9/11.
- `npm run build` and focused ESLint pass. Mandated runtime capture is in `output/detective-side-gait-restored/`. Targeted frame sampling in `output/detective-side-gait-frames/` confirms left cycles through 1/7 and right through 3/5; both screenshots were visually inspected for correct facing and metadata reports zero browser errors.

NPC facing-state follow-up (current prompt): idle NPCs now default to atlas frame 0 (forward), travel continues to use movement direction, and any conversation-facing command overrides the idle default. Interview guests therefore always select the direction toward the player while the detective faces the guest.
- Added robust side-profile mirroring because several generated atlases contain same-facing art in both nominal profile columns. Conversation and travel now use one canonical side frame and mirror it toward the actual target.
- `npm run build` and focused ESLint pass. Mandated runtime capture is in `output/npc-facing-final/`. Targeted left/right interview verification in `output/npc-facing-player-final/` confirms frame 1 with flip `1` for a player on the left and `-1` for a player on the right; screenshots were visually inspected and metadata reports zero browser errors.
- Side-approach correction: inverted the canonical profile's screen-space flip after gameplay feedback showed the previous mapping made NPCs look away from left/right approaches.
- `npm run build` and focused ESLint pass. Mandated runtime capture is in `output/npc-side-facing-fix/`. Targeted interview captures in `output/npc-side-facing-final/` visually confirm the magician faces the detective from both sides; metadata records flip `-1` for a left-side approach and `1` for a right-side approach with zero browser errors.
- Per-archetype facing correction: canonical atlas profile orientation is inconsistent—accountant and antiquarian face right in frame 1, while the other eight archetypes face left. Replaced the global flip assumption with identity-aware mirroring toward the target.
- `npm run build` and focused ESLint pass. Mandated runtime capture is in `output/npc-profile-aware-facing/`. Targeted all-archetype verification in `output/npc-profile-facing-all/meta.json` confirms each NPC uses the correct identity-specific flip for targets on both sides, with zero browser errors. Canonical profile reference was visually inspected in `output/npc-canonical-profiles.png`.
- Accountant-specific correction: gameplay confirmed the accountant's canonical frame 1 faces left, not right. Antiquarian is now the sole right-facing canonical profile; accountant uses the same left-facing rule as the other eight archetypes.
- `npm run build` and focused ESLint pass. Mandated capture is in `output/accountant-facing-runtime/`; focused accountant assertions in `output/accountant-facing-final/meta.json` confirm frame 1/flip `1` for a player on the left and frame 1/flip `-1` for a player on the right, with zero browser errors.

Detective sprite follow-up (current prompt): create a detective sprite matching the existing NPC art, with directional, walking, turning, and investigating poses, then replace the geometric player model.
- Generated a consistent 16-pose 4x4 detective atlas against the existing ten NPC portraits, removed the chroma key to alpha, and saved the project assets under `public/assets/characters/`.
- Replaced the visible detective geometry with an atlas-driven 2.5D cutout. Runtime frame selection now covers four idle directions, two four-direction walk cycles, two turning poses, and two investigating poses.
- `npm run build` and focused ESLint pass. The mandated browser client started a live case and exercised movement/turning with deterministic steps; `output/detective-sprite-runtime-2/` was visually inspected. The new detective matches the NPC cutout scale and painted style, changes pose with heading/gait, remains grounded, and produced no console/page error artifact.
- Direction correction follow-up: the camera views the world from +Z while the model's zero heading points toward -Z, so the atlas lookup was half a turn out. Added the required 180-degree offset: north uses the back pose, south the front pose, east the right-looking silhouette, and west the left-looking silhouette.
- Direction verification: focused ESLint passes. Four isolated mandated browser runs were visually inspected in `output/detective-direction-up/`, `-down/`, `-left/`, and `-right/`; each final pose faces its actual travel direction and no browser error artifact was produced. Full build is currently blocked by unrelated existing unused declarations in `src/game/audio.ts` (`EIGHTH_MS`, `buildSoftDelay`, and `playCompositionStep`).

Murderer aggression follow-up: user reported that the murderer was not eliminating NPCs and asked for fast pursuit subject to no detective and no more than one witness.
- Root cause: legal kills still required a low-probability roll on infrequent idle ticks, hunting only happened 40% of the time, and cooldowns lasted 55-80 game minutes.
- Changed legal opportunities to deterministic kills, added an arrival-time opportunity check, made low-occupancy prey hunting the killer's default decision, and shortened repeat cooldowns to 20-30 game minutes. Legal rooms contain the victim plus at most one additional witness, with the detective elsewhere.
- Fixed movement recovery so repeated sideways shuffling actually triggers a detour; this was trapping the killer at a doorway even after the hunting logic selected a target.
- Verification: focused ESLint and production build pass. Deterministic rule assertions cover legal kills, detective presence, and excessive witnesses. The full-night simulation now produces 7 deaths for the fixed smoke-test seed (previously 0). The mandated browser client started a live case without errors; `output/web-game/shot-0.png` and matching state were inspected.
- Cooldown pacing follow-up: increased the repeat-murder cooldown from 20-30 to a randomized 30-45 game minutes and added a focused range assertion.

NPC traffic-jam follow-up (current prompt): stuck NPCs should find a new direction and resume traveling.
- Added per-NPC blocked/progress tracking and randomized, alternating recovery detours. After 0.65 seconds without meaningful progress toward its waypoint (including fruitless sideways shuffling), a guest temporarily sidesteps/backtracks into nearby open space and then resumes the original waypoint path.
- If no nearby recovery direction is open, the congested route is abandoned and the NPC immediately makes a fresh movement decision instead of remaining permanently stuck.
- Verification: `npm run build` and focused ESLint pass. The mandated web-game client advanced a live case through NPC decision/movement time with no browser error artifact; inspected capture and state are in `output/npc-stuck-recovery-final/`.

Current evidence follow-up: investigating a dead body should yield one deterministic, archetype-based clue; every archetype needs three possible clues and every clue must overlap multiple archetypes.
- Added ten semantic evidence types. Each clue is shared by exactly three archetypes, and each of the ten archetypes has exactly three possible traces.
- Murder scenes select from the killer archetype's fixed three-clue sequence; bodies can be examined once at close range with E, adding an Evidence lead that names three possible profession sources.
- Body investigation takes interaction priority over interviewing a nearby living guest.
- Verification: build, focused ESLint, simulation evidence assertions, mandated web-game client, and targeted browser investigation all pass. `output/evidence-investigation/journal.png` was visually inspected; metadata confirms the clue included the actual killer archetype among three candidates and no browser errors occurred.
- Chalk-outline follow-up: after evidence is collected, the victim model is hidden and replaced in place by a persistent floor-level chalk silhouette; text-state output distinguishes bodies from investigated chalk outlines.
- Chalk-outline verification: production build, focused ESLint, simulation smoke test, mandated browser client, and a targeted before/after investigation pass all succeed. `output/chalk-outline-final/after.png` was visually inspected and clearly shows the detective beside the replacement outline; metadata reports `appearance: "chalk outline"` with no browser errors.

Current collision follow-up: characters should not pass through one another or major room objects.
- Added shared collision footprints for major furniture, actor-radius movement checks for the detective and NPCs, collision-aware NPC steering, and valid spawn/destination sampling.
- Verification: `npm run build` and focused ESLint pass. The mandated browser client confirmed the detective stops at the expanded dining-table footprint and newly spawned NPCs remain on open floor; inspected artifact: `output/collision-verification-final/shot-0.png`. No browser error artifact was produced.

Current follow-up prompt: the character models should change which direction they are facing based on movement or conversation

Side-walk leg animation follow-up (current prompt): traced the lateral atlas mapping to a row/column mix-up. The renderer was alternating the left- and right-facing cells within one walk row and mirroring them, which preserved essentially the same leg pose. Side movement now keeps the requested direction column fixed and alternates between the two authored walk rows, where the genuinely opposing leg strides live, for both the detective and every guest atlas.
- Verification: production build and focused world ESLint pass. The mandated client completed in `output/side-walk-leg-runtime/`. Deterministic left-walk captures in `output/side-walk-leg-targeted/` sampled frames 7 and 11: the direction column remained fixed while the stride row changed, visibly exchanging the detective's forward and rear legs. Right movement likewise sampled frames 9 and 5. Screenshots were visually inspected and the targeted run reported zero console/page errors.
- Whole-pose flicker correction (current prompt): the two authored stride rows also contained small head, torso, arm, and prop differences, making characters appear to flip their entire pose. At texture load time, the second walking row now inherits the first walking row's upper 70% for every direction; only the lower 30% containing legs and feet differs between stride frames.
- Whole-pose correction verification: normalized all generated atlases to whole-pixel cells before composition, preventing blended row seams. A pixel audit across the detective and all ten live guest textures reports zero differing upper-body pixel channels between stride frames. Production build, focused ESLint, and the mandated client pass; deterministic frame 7/11 captures in `output/leg-only-walk-targeted-final/` were visually inspected, show the same upper silhouette with alternating legs, and report zero browser errors.
- Lateral leg alignment follow-up (current prompt): the authored lower halves still came from independently drawn poses and did not align for most characters. Lateral stride B is now derived entirely from stride A: its upper 70% is copied exactly and its bottom leg region is mirrored in place, avoiding cross-pose seams and ensuring the feet exchange while the character keeps one stable facing pose.
- Lateral leg alignment verification: production build and focused world ESLint pass. The mandated live client completed in `output/lateral-leg-alignment-runtime/` without browser errors. A runtime-generated contact sheet in `output/lateral-leg-alignment-targeted/` was visually inspected across the detective and all ten randomized guest models in both left and right phases. Every actor reports zero upper-body pixel differences and a nonzero leg-region difference between phases; the detective now clearly exchanges its leading foot without changing its facing silhouette.

Standardized character-atlas rebuild (current prompt): removed all runtime mirroring and body-part splicing. Added an explicit shared atlas contract in `src/game/characterAtlas.ts` (front/right/back/left columns; idle/walk-A/walk-B/action rows) and a reproducible Pillow builder in `scripts/rebuild_character_atlases.py`.
- Generated eleven non-destructive `*-atlas-v2.png` sheets at 320px per cell. All locomotion poses retain their fully authored opposing strides, share a 294px foot baseline, and normalize to a nominal 270px standing height. Measured idle cells land within 272–276 visible pixels across the entire cast after antialiasing.
- Detective action correction: the kneeling investigation cell is registered at 72% of standing height (198 measured pixels) while turn and notebook poses use standing scale. Restored the crouch-to-notebook action sequence at fixed sprite-root scale, eliminating the oversized crouched pose.
- Verification: production build, focused ESLint, and atlas-builder syntax check pass. The mandated browser client completed in `output/standardized-atlas-runtime/`. The all-character left/right A/B contact sheet in `output/standardized-atlas-targeted/contact-sheet.png` and crouch/notebook gameplay captures were visually inspected. Runtime sampled frames 14 and 15 at sprite scale 1 and reported zero console/page errors.

Detective passing-frame follow-up (current prompt): added registered feet-together side-profile cells to the detective atlas and changed lateral walking to four beats: stride A, passing, stride B, passing. Corrected the passing-frame direction lookup after live capture showed the two visual facings were labeled oppositely in the runtime mapping.
- Verification: production build, focused ESLint, and the mandated client pass. Targeted middle-frame captures in `output/detective-passing-frame-targeted/` were visually inspected: frame 12 faces left and frame 13 faces right while both keep the feet together. Left/right runtime metadata reports zero console/page errors.

War correspondent facing follow-up (current prompt): the correspondent's authored side-profile columns are opposite to the shared atlas convention. Added an explicit archetype direction-column override so both walking and stopped conversation-facing poses select the correct left/right artwork without mirroring the sprite.
- Verification: production build, focused ESLint, and mandated client pass. The live-texture audit in `output/correspondent-facing-targeted/contact-sheet.png` was visually inspected: left movement selects frames 5/9 and both face left; right movement selects frames 7/11 and both face right. Metadata reports zero console/page errors.

NPC v3 atlas team follow-up (current prompt): a three-agent image-generation team regenerated all ten guest sheets from their v2 identities using the detective v2 as the scale/structure reference. The shared v3 contract is 4 columns (front/right/back/left) by 6 rows (idle/stride A/passing/stride B/actions/portraits), 320px cells, transparent RGBA, nominal 270px locomotion height, and a shared 294px baseline.
- Added `NPC_ATLAS_V3` runtime/schema metadata plus normalization and validation scripts. NPC runtime now uses the same A → feet-together → B → feet-together four-beat gait as the detective, with six-row death/outline indices migrated correctly. The correspondent's v3 sheet follows the standard columns, removing its prior runtime exception.
- Root QA rejected the agents' first assembled contact sheet because several generated figures crossed nominal cell boundaries, leaving detached hats/feet. Reassembled the affected locomotion rows from pristine full-sheet alpha sources using detected authored row bands, then reran the validator and visually inspected `output/npc-v3-atlas-final/contact-sheet-clean-2.png`; all 60 side gait cells are now clean and registered.
- Surgeon direction correction: its generator authored both profiles facing left. Kept the left artwork and authored the right locomotion column as its full-sprite horizontal counterpart inside the atlas, so runtime remains contract-driven. `output/npc-v3-atlas-final/surgeon-directions-final.png` confirms both A and passing poses face correctly.
- Verification: all ten sheets pass 1280x1920 RGBA, alpha, 16-cell locomotion occupancy, 250–285px height, and baseline validation. Production build, focused ESLint, Python syntax checks, and mandated runtime pass. Live gameplay loaded all ten v3 atlases at six rows with zero console/page errors; inspected capture and metadata are in `output/npc-v3-runtime-targeted/`.
- Full direction audit follow-up: enlarged A/passing/B review exposed additional model-output inconsistencies. Corrected surgeon, curator, columnist, magician, vocalist, chauffeur, accountant, debutante, and correspondent directly in their atlases so every left trio faces left and every right trio faces right; no runtime archetype exceptions remain. Correspondent A/passing columns were swapped while its already-correct B row was retained, preventing its back-and-forth facing switch. Curator's missing right poses were authored as full-sprite counterparts of its left poses, eliminating backward rightward movement. Final visual reference: `output/npc-v3-atlas-final/direction-audit-final.png`; targeted surgeon/correspondent/curator sheets are alongside it.
- Final atlas cleanup removed debutante row bleed and reran validation plus the mandated browser client in `output/npc-v3-runtime-final-2/`; no browser error artifact was produced.

Name/gender constraint follow-up: user requested that randomized guest names match the gender conveyed by each established character sprite.
- Classified columnist, curator, vocalist, and debutante as female; surgeon, magician, correspondent, accountant, antiquarian, and chauffeur as male based on the supplied sprites.
- Split randomized names into gendered pools and assign from the matching pool after archetypes are shuffled.
- Verification: `npm run build`, focused ESLint, and `sim-test.ts` pass. The mandated browser client started a live randomized case with no browser errors; its screenshot/state are in `output/name-gender-verify/` and were visually inspected.
- Expanded the randomized roster from 5 female/6 male names to 40 names in each pool, preserving the sprite-gender constraint while greatly increasing case-to-case variety.
- Verification for expanded pools: confirmed all 80 names are unique, `npm run build` and focused ESLint pass, and the mandated live browser check was visually inspected in `output/name-pool-expanded/` with no console/page error artifacts.

Character facing follow-up:
- Added persistent, smoothly interpolated actor headings. Movement now turns each model toward its actual x/z travel direction instead of the walk animation overwriting Y rotation with sway.
- NPCs in simulated conversations face their talk partners; during interviews the detective and guest turn toward one another.
- Kept walk motion as a subtle body-only sway so it no longer destroys the actor heading.
- Verification: `npm run build` and focused ESLint pass. The mandated web-game client ran without browser errors and its inspected gameplay capture is in `output/character-facing-movement/`. A targeted browser check confirmed opposite headings for north/south movement and opposing player/guest headings during an interview; the inspected interview capture is `output/character-facing-movement/interview.png`.

Notes:
- Found the soundtrack in `src/game/audio.ts`; it is procedural WebAudio with no external assets.
- Current likely fatigue sources: bright rain noise, loud static pad, sharp square tick, and sawtooth dissonant stingers.

TODO:

Built-in interview coherence follow-up (current prompt): removed the disconnected character story graphs from Built-in Dialogue runtime. Built-in interviews now ask a stable set of case-grounded questions about the current victim, timeline, alibi, witnessed activity, conversations, and suspicion; answers come from the current simulation's room history, rumors, and contact history. Once the detective has collected a physical trace, relevant guests receive a direct association question that reveals only that canonical connection. Asked questions remain visible but disabled, preventing circular repetition.
- Static verification: production build, focused game/simulation ESLint, and diff check pass.
- Runtime verification: the mandated web-game client completed in `output/dialogue-coherence-runtime/`. A targeted live interview in `output/dialogue-coherence-targeted/` confirmed victim-specific questions, a simulation-grounded timeline answer, disabled asked-question state, and zero console/page errors; the final dialogue screenshot was visually inspected.

Narrative story-graph overhaul (current prompt): replaced the built-in two-stage advance/stall/close interviews with ten character-owned noir story graphs written by a three-agent writers' room. Every archetype now has a distinct private compromise, durable trust/pressure/flag state, non-repeatable choices, alternate routes, 3–5 reachable endings, and all three canonical evidence associations unlockable through justified observations, bargains, protection, reconstruction, or pressure.
- Added `src/game/stories/storyPackA.ts`, `storyPackB.ts`, and `storyPackC.ts`, plus the normalized runtime adapter in `storyCatalog.ts`. Built-in mode and failed-LLM fallback use the story graphs; successful LLM plans remain supported.
- Added story state to `render_game_to_text` and enlarged the interview panel so longer authored beats and three consequential responses remain visually separated at gameplay resolutions.
- Research direction drew on noir's moral/psychological ambiguity, compromised protagonists, fatal consequences, class/power pressure, and endings that resist tidy moral closure; plots and language remain original.
- Verification: exhaustive `story-graph-test.ts` explores reachable states for all ten archetypes and passes with 37 endings and all 30 archetype/evidence associations reachable. Production build and targeted runtime pass. The live Columnist interview in `output/story-runtime-targeted/` shows the authored opening and three weighted choices with zero browser errors; screenshots were visually inspected.

First-contact dialogue and opening-premise correction (current follow-up): removed writer-room story titles and undisclosed private facts from the initial interview UI. Every archetype now offers at least three first-contact questions grounded in observable clothing, objects, behavior, or events; the Correspondent's premature source question was rewritten as an observation about the pocket he keeps checking. Graph validation now rejects fewer than three opening questions, non-questions, and internal story-title leakage.
- The game now begins after a murder has already occurred. `Simulation.createOpeningCrime()` selects a non-killer victim, creates killer-linked scene evidence, marks the body discovered, and moves the murderer away before play. The title, opening log, and first journal lead explicitly say the detective was summoned by this death before the storm sealed the roads. Gameplay begins with nine living suspects and one known body ready to investigate.
- Verification: production build, focused ESLint, simulation smoke test, exhaustive story graph test, diff check, mandated web-game client, and targeted browser passes all succeed. All ten opening-question sets were captured in `output/dialogue-openers-targeted/meta.json`; the opening case reports nine alive/one body with a killer-linked clue and zero browser errors in `output/opening-murder-targeted/`. Both UI captures were visually inspected.
- Replaced the soundtrack mix with a softer, longer-form procedural noir bed: rain, warm pad, slow bass pulse, sparse melody, softer thunder/tick/stingers.
- Added `window.render_game_to_text` so browser verification can record concise gameplay state.
- `npm run build` passes after the audio changes.
- Web-game Playwright verification passes after installing Playwright for the verifier; artifacts are in `output/music-verify/`.
- `npm run lint` still fails on pre-existing UI component template issues outside this task (`react-refresh/only-export-components` and `react-hooks/purity` in shared `src/components/ui/*` files).

TODO:
- Listen in a real browser with speakers/headphones and fine-tune musical taste if desired; automated checks can prove runtime health, but not taste.
- Follow-up audio request: user still heard an unpleasant siren-like quality and asked for more structured, intriguing music that supports deep thought and mystery.
- Replaced the continuous gliding pad approach in `src/game/audio.ts` with a structured 66 BPM procedural score: repeating chord progression, bass clue, chord shadow, sparse mallet arpeggios, and short question/answer phrases.
- `npm run build` and `npx eslint src/game/audio.ts` pass after the structured score change.
- Web-game Playwright verification passes with artifacts in `output/music-verify-structured/`; no console/page error files were produced.

Camera transition note (current prompt): user reported the camera angle swing on room entry felt jarring and did not read as a clean space-to-space transition.
- Updated `src/game/world.ts` so camera look-at eases toward its desired focus instead of snapping immediately.
- Added doorway/passages focus weighting via `trackPlayer`, so the camera follows the player near thresholds and settles back to the room center once inside.
- Added `window.render_game_to_text` debug output in `src/game/game.ts` for automated verification.
- Verification: `npm run build` passes. Production-build Playwright check against `http://127.0.0.1:8000` crossed from Dining Hall into Ballroom with no console errors; final state in `output/camera-transition-door-2/state-0.json` showed player in Ballroom and camera focus biased smoothly between the entry-side player and room center.

Room readability note (current prompt): user reported muddy room details, too many indoor particles, and walls/furniture visually conflicting with doorways.
- Increased the Three.js render-target resolution from 0.32 to 0.58 and reduced scene fog.
- Reduced indoor dust from 90 bright motes to 22 subtle motes.
- Reduced rain count/opacity/size and sampled rain outside covered room/passage areas so it no longer falls through interiors.
- Added subtle box edge outlines to floors, walls, passages, thresholds, and furniture to improve silhouettes.
- Fixed wall segment math so doorway-side walls run from room corners to the door gap, then added threshold strips so openings read intentionally.
- Lowered wall height from 2.7 to 1.85 to reduce top-down doorway occlusion.
- Split library shelves away from north/east/west doorway bands so shelves no longer visually sit over openings.
- Softened the app-level vignette and scanline overlay.
- Added `window.advanceTime` and `window.murderMansionGame` alongside the existing `window.render_game_to_text` hook.

LLM request-load correction (current prompt): repeated Groq 429 responses were caused by every idle NPC decision tick spawning its own chat-completion request while interview generation used the same endpoint.
- NPC movement, purposeful room choice, social conversations, murderer pursuit, kills, and recovery pathing now always use the built-in deterministic simulation in both dialogue modes.
- Removed the ambient LLM decision interface and request orchestration entirely; the configured provider is now reserved for the single conversation-plan request initiated when the player starts an interview.
- Renamed player-facing mode copy from whole-game/AI-director language to Built-in Dialogue and LLM Dialogue, explicitly stating that behavior and case logic remain local.
- Production build, focused ESLint, and diff checks pass. The mandated web-game client reached and visually verified the revised setup screen in `output/llm-dialogue-only-runtime/` with no browser error artifact.
- Verification: `npm run build` passes. Web-game checker completed against `http://127.0.0.1:3001/`; inspected `output/visual-rooms-2/shot-0.png`, `output/visual-east-door/shot-0.png`, `output/visual-north-door/shot-0.png`, and `output/visual-east-transition/shot-0.png`. Latest targeted runs produced no error logs.
- Follow-up suggestion: horizontal doors are centered at z=0 while the Dining Hall start is z=2, so holding right from the start correctly hits a wall. If players keep trying that, add a stronger floor/path cue toward the east/west doorway centerline.

Journal portrait note (current prompt): user asked for each guest NPC journal entry to use the corresponding character from `character-sprite-reference-alpha.png`.
- Cropped the ten guest archetypes into transparent PNG assets under `public/assets/characters/`.
- Added archetype IDs to guest snapshots so randomized guest assignments always resolve to the correct portrait.
- Updated guest cards and interview transcript rows to render the matching portrait, with a subdued treatment for deceased guests.
- Made the journal header and guest-card grid responsive so portraits remain readable on narrow screens.
- Verification: `npm run build` and focused ESLint checks pass. Web-game runtime verification and desktop/mobile journal screenshots pass with all ten unique portraits loaded and no browser errors; artifacts are in `output/journal-portraits-runtime/` and `output/journal-portraits-ui-final-2/`.

Room rework note (current prompt): user asked for a designer subagent to create distinct, readable concepts for all nine rooms, followed by a developer subagent implementing the approved handoff.
- Current room set: Study, Gallery, Conservatory, Kitchen, Dining Hall, Ballroom, Cellar, Library, and Master Suite.
- Workflow underway: inventory/current screenshot review -> designer room-by-room concept and shared visual language -> developer implementation -> build and Playwright visual verification.
- Developer pass: added cached shared materials plus reusable inlay/rug/table/chair/shelf/frame/candle/plant/barrel primitives in `src/game/world.ts`; implemented nine distinct floor languages and room-specific landmark/prop/mystery compositions. Furniture remains visual-only and `isWalkable` is unchanged.
- Room rework verification: `npm run build` passes. The mandated web-game client successfully captured `output/web-game/` (Dining Hall) and `output/room-rework-gallery/` (north transition into Gallery); both screenshots were opened and inspected. Dining's herringbone/rug/table/candle composition reads cleanly with visible north/south space and distinct adjacent Kitchen checker/Ballroom medallion floors. Gallery's central portrait, unequal sculptures, runner, bench, and door lanes read clearly; state confirms the player transitioned into Gallery and no console/page error artifact was produced. Longer chained traversals were too slow for the verifier's execution window, so lower-row rooms were reviewed through the central neighboring-room framing and build/runtime health rather than a completed chained artifact.
- Room rework follow-up suggestion: if a future pass needs isolated close-ups of every room, add a debug-only room teleport/query hook or run one short doorway transition per capture; the mandated client's per-frame choreography is slow for two consecutive 13-unit crossings.
- Kitchen floor artifact follow-up: the original dense one-unit checker grid aliased into bands at the low-resolution pixel pass, and its two route-strip meshes were coplanar with the tiles/each other. Replaced it with broader, lower-contrast quarry tiles and raised non-overlapping route-strip pieces. `npm run build` passes; the mandated client traversed Dining Hall -> Kitchen and produced `output/kitchen-floor-fix-direct/`. The screenshot was inspected at full resolution, state confirms the Kitchen, and no console/page error artifact was produced.
- Shared floor-lighting artifact follow-up: remaining circular effects in Kitchen, Study, and Conservatory were point-light falloff gradients quantizing through the low-resolution nearest-neighbor render target. Added ACES filmic output and softened practical-light falloff, then made room floor surfaces/inlays use cached flat materials so dynamic lighting cannot paint rings across them; walls, furniture, actors, and bulbs remain dynamically lit. `npm run build` passes. Mandated client traversals and inspected screenshots cover all affected rooms in `output/floor-flat-fix-kitchen/`, `output/floor-flat-fix-study/`, and `output/floor-flat-fix-conservatory/`; state confirms each destination and none produced error artifacts.

Character model note: user asked for the in-world character models to clearly match their corresponding journal sprites.
- Reworked all ten guest models around their archetype IDs instead of giving every guest the same body and hat with a randomized color.
- Added portrait-matched palettes, silhouettes, hair/headwear, and signature props: columnist hat/feather, surgeon coat, curator garden hat/apron, magician top hat/cape, correspondent camera/satchel, accountant glasses/ledger, antiquarian beard/magnifier, chauffeur peaked cap/brass buttons, debutante gown/tiara, and vocalist gown/hair/flower/microphone.
- Kept the detective model and generic fallback intact.
- `npm run build` and focused ESLint checks pass.
- Automated all-character lineup and journal comparison artifacts are in `output/character-models-final/`; all ten models rendered and no console/page errors were recorded.

Dialog portrait follow-up (current prompt): user asked for guest sprite portraits to appear in the interview dialog card too.
- Extracted the archetype portrait mapping and renderer into a shared `GuestPortrait` component used by journal cards, transcript rows, and the interview dialog.
- Reworked the dialog header so a larger portrait sits beside the guest identity and current response without increasing the card's overall height.
- Verification: `npm run build`, focused ESLint, and the web-game client pass. Desktop/mobile dialog screenshots and a live question/answer interaction are in `output/dialog-portrait-ui/`; all ten archetype mappings load correctly with no browser errors.

Full character-model redesign (current prompt): designer handoff is in `CHARACTER_MODEL_DESIGN.md` and covers ten portrait-matched guests plus the detective.
- Replaced the shared box-body actor construction with layered model assemblies, reusable limb/prop pivots, separated legs/shoes/arms/hands, tapered skirts/coats, portrait palettes, distinct headwear/hair, and oversized signature props for all ten archetypes.
- Rebuilt the detective with a split belted trench silhouette, shirt/tie/lapels, badge, pinched medium-brim fedora, notebook, and flashlight.
- Added restrained opposing limb walk motion and archetype-sensitive idle prop/head gestures while preserving actor positions, visibility, labels, death state, and gameplay simulation APIs.
- Verification: `npm run build` and `npx eslint src/game/world.ts` pass. The mandated web-game client started a live case at `http://127.0.0.1:3004`, advanced deterministic frames, and captured `output/character-redesign/shot-0.png` plus matching `state-0.json`; the screenshot was opened and inspected. The detective and nearby guest render stably in the Dining Hall with labels and props attached, and no console/page error artifact was produced. There is no existing all-character lineup/debug teleport, so this focused runtime capture covers the normal gameplay camera rather than all eleven models simultaneously.

LLM provider presets (current prompt): user asked to configure a free default option for LLM-driven characters.
- Added Groq Free, Ollama Local, and Custom provider presets. Groq is the recommended/preconfigured hosted choice when LLM mode is selected; built-in minds remain the reliable out-of-box director.
- Groq defaults to `https://api.groq.com/openai/v1` with `llama-3.1-8b-instant`. Ollama defaults to `http://localhost:11434/v1` with `llama3.2`.
- Ollama can now run without a fake API key, and the LLM client omits the Authorization header when no key is present.
- Preserved old saved endpoint configurations by migrating them to the Custom preset.
- Focused ESLint passes for all changed files. Web-game client screenshots were inspected for Groq and Ollama in `output/llm-provider-settings/` and `output/llm-provider-ollama-2/`; no browser error artifacts were produced.
- Full `npm run build` is currently blocked by an unrelated existing `src/game/world.ts` Actor type error (the object at line 665 lacks limb/animation fields). This provider work did not modify that file.

GitHub Pages portrait-path fix (current prompt): character sprites loaded locally but not from the deployed project subpath.
- Root cause: `GuestPortrait.tsx` used root-absolute `/assets/characters/...` URLs, which bypassed the repository subpath on GitHub Pages.
- Updated portrait URLs to prepend Vite's `import.meta.env.BASE_URL`, preserving local development while resolving correctly under the deployed base.
- Verification: `npm run build` passes. The mandated browser client successfully loaded gameplay from the nested `/murder_mansion/dist/` path with no browser error artifact. A targeted journal check loaded all ten sprite images at nonzero natural widths with no console errors; `output/github-pages-sprite-fix-final/journal.png` was visually inspected.

Guest profile card sizing follow-up (current prompt): normalize guest card dimensions and spacing, especially for deceased guests whose missing suspicion/accusation controls pulled their evidence rows upward.
- Guest grid rows now share one intrinsic height; every card fills its row as a flex column, and the associated-evidence section anchors to the bottom so living and deceased profiles keep consistent dimensions and vertical rhythm.
- Verification: production build and focused Journal ESLint pass. The mandated web-game client completed in `output/guest-card-runtime/`. Targeted mixed living/deceased coverage in `output/guest-card-targeted/` measured all ten cards at the same 341 × 244.5 px, with every evidence section sharing the same top and bottom offsets; no console/page errors were reported.

Animated in-world sprite follow-up (current prompt): user found the geometric character models too blocky and asked to turn the actual character sprites into animated models.
- Replaced the visible geometric guest assemblies with the same transparent full-body artwork used by the journal. The underlying actor/gameplay rig remains intact, while the rendered guest is now a depth-tested 2.5D cutout with a floor shadow.
- Added procedural walk bounce, compression, lean, idle breathing/sway, camera-facing billboarding, and movement-heading mirroring. The detective remains the authored 3D player model because no detective sprite asset exists.
- Death state now desaturates/dims the sprite and preserves the existing fallen pose and chalk-outline workflow.
- `npm run build` and focused ESLint pass. The mandated web-game client completed two runtime scenarios with deterministic frame advancement and no console/page error artifacts. Screenshots and matching state are in `output/animated-sprite-characters/` and `output/animated-sprite-idle-walk/`; both sets were visually inspected. Guests render as crisp, recognizable sprite cutouts with grounded shadows and stable labels at the normal isometric camera scale.
- Follow-up option: a true multi-frame hand/leg animation would require drawing or generating additional per-character frames; the current source sprites contain one pose each, so this pass uses procedural whole-character motion and mirroring.

Lower camera-angle follow-up (current prompt): user liked the sprite actors but found their viewing angle too steep.
- Lowered the camera height from 15.5 to 12.6 world units, moved it farther forward from 8.6 to 10.4, and widened FOV slightly from 46° to 47°. This changes the view from a steep floor-dominant angle to a more character-forward composition while keeping approximately the same room coverage.
- `npm run build` and focused ESLint pass. The mandated browser client verified the normal Dining Hall view and a south-door camera transition; screenshots/state are in `output/lower-camera-sprites/` and `output/lower-camera-transition/`. All captures were visually inspected: sprites read more upright, room coverage remains clear, foreground walls do not obscure the player, labels remain stable, and no browser error artifacts were produced.

Second camera-height follow-up (current prompt): user requested one more subtle downward adjustment.
- Lowered camera height from 12.6 to 11.7 while preserving the 10.4 forward offset and 47° FOV, producing a modestly more upright character view without changing horizontal framing.
- `npm run build` and focused ESLint pass. The mandated client traversed Dining Hall to Library at the new height; `output/lower-camera-final/` was visually inspected. Sprites read more upright in both rooms, the detective and doorways remain visible, foreground walls do not interfere, and no browser error artifacts were produced.

Authored MP3 soundtrack follow-up (current prompt): user added `Midnight in the Static.mp3` at the project root and asked to use it as background music.
- Replaced the procedural musical composition with the authored MP3, bundled through Vite and looped through the existing Web Audio music bus. Procedural rain, thunder, clock tick, and event stingers remain intact.
- Existing master volume and mute controls continue to govern the full mix; autoplay retry remains tied to subsequent user gestures.
- Removed the obsolete procedural score code and slightly lowered the music-bus gain so ambience and gameplay cues remain audible beneath the track.
- Verification: production build and focused ESLint pass. The mandated web-game client started a case and its inspected gameplay capture is in `output/mp3-soundtrack/`. A targeted browser check confirmed one loaded MP3 element, active playback, a 119.96-second duration, looping enabled, advancing play time, working mute UI, and no console/page errors.
- SFX audibility follow-up: the effects were connected but substantially masked by the song, with rain at a near-inaudible gain, thunder delayed 26-68 seconds, and clock ticks occurring only every 60 real-time seconds at normal speed. Lowered the music bed, raised ambience/SFX/rain and individual cue levels, scheduled the first thunder for 4-6.5 seconds, moved clock ticks to each in-game quarter hour, and added brief music ducking for story stingers.
- Re-verification: production build and focused ESLint pass. The mandated client ran seven seconds into a live case; `output/sfx-rebalance/shot-0.png` was visually inspected with no browser errors. Targeted Web Audio instrumentation confirmed looping rain plus thunder buffer sources start, advancing to the quarter-hour starts the clock oscillator, and no console/page errors occur.
- Authored clock follow-up: replaced the synthesized quarter-hour oscillator with the root-level `ticktock.mp3` as a continuous loop at 10% gain on the ambience bus. It starts and retries alongside the soundtrack, follows the existing master volume/mute setting, and is cleaned up with the other audio elements when the game is disposed.
- Clock-loop verification: production build and focused ESLint pass; Vite bundles the 524 KB clock asset. The mandated client started a live case and `output/ticktock-loop/shot-0.png` was visually inspected. A targeted browser check confirmed both the 278.96-second soundtrack and 16.38-second clock file are loaded, actively advancing, loop-enabled, mute-controlled, and produce no console/page errors.
- Game-time clock synchronization follow-up: mapped one second of `ticktock.mp3` to one simulated minute, continuously correcting small phase drift. The clock playback rate now follows the selected 0.5x/1x/2x/4x game speed, pauses when the case is paused or outside gameplay, and re-aligns to simulation time on resume.
- Synchronization verification: production build and focused ESLint pass. The mandated client capture in `output/clock-game-time-sync/` was visually inspected. A targeted browser sequence confirmed clock audio at 1x tracks the fractional game minute, switches to playbackRate 4 at 4x, remains exactly stationary while paused, resumes aligned to the new game minute, and produces no console/page errors.
- Authored body-discovery cue follow-up: replaced the synthesized body stinger with root-level `chord.mp3`, routed at controlled gain through the existing SFX bus. Body discovery now restarts the 1.896-second cue from its beginning and ducks the soundtrack for three seconds; accusation/win/lose cues remain synthesized and unchanged.
- Body-cue verification: production build and focused ESLint pass; Vite bundles the 37 KB cue. The mandated live-case capture in `output/body-discovery-chord/` was visually inspected. A targeted browser test confirmed the cue preloads, is non-looping, advances when `body` fires, restarts near zero on a repeated trigger, creates no legacy oscillator for body discovery, and produces no console/page errors.
- Authored rain follow-up: replaced the continuous procedural rain-noise source with root-level `rain.mp3`. The 30.024-second track loops through gentle high/low-pass filtering at 12% gain on the ambience bus; generated noise remains allocated only for the separate thunder effect.
- Rain verification: production build and focused ESLint pass; Vite bundles the 938 KB rain asset. The mandated live-case capture in `output/authored-rain/` was visually inspected. A targeted browser check confirmed soundtrack, synchronized clock, and rain are all loaded and advancing as independent loops, the body chord remains preloaded/non-looping, no procedural buffer source starts before thunder, mute still controls the mix, and no console/page errors occur.
- Player-footsteps follow-up: added root-level `footsteps.mp3` as a preloaded 10.872-second loop at 34% gain on the SFX bus. It starts only when the detective's world position actually changes, stops when movement input is released or collision prevents motion, and is forced off outside active play (including interviews and pause).
- Footsteps verification: production build and focused ESLint pass; Vite bundles the 212 KB asset. The mandated client ran after the change, and the targeted gameplay capture `output/player-footsteps/walking.png` was visually inspected. Browser state checks confirmed footsteps are paused at idle, advance while the detective moves, remain stationary after release, stop on pause even with a movement key held, loop correctly, and generate no console/page errors.
- Audio audibility correction: decoded-level analysis found the authored rain averages -45.6 dBFS versus the soundtrack's -16.2 dBFS, while footsteps average -29.4 dBFS. The earlier 12% rain and 34% footstep gains compounded that source-level mismatch, leaving rain roughly 40 dB below the score and footsteps about 14 dB below it.
- Raised rain gain to 1.6 (still approximately 17 dB beneath the score after mixing) and footsteps gain to 1.35 (about 2.5 dB beneath the score). Reworked thunder with a 150-280 Hz presence layer and brief music ducking while preserving its deep rumble, so it survives laptop/small-speaker playback.
- Re-verification: production build and focused ESLint pass. The mandated client ran after the rebalance; targeted live gameplay confirmed rain is continuously loaded/advancing, footsteps start and stop with actual player movement, and thunder starts its buffer source. `output/audio-audibility-rebalance/gameplay.png` was visually inspected and no console/page errors occurred.
- Additive-mix correction: removed the shared master dynamics compressor, whose -30 dB threshold caused newly audible rain/footsteps to reduce the entire mix, and removed the explicit music duck from thunder. Ambient rain, synchronized clock, footsteps, and thunder now layer additively without changing the music-bus gain; intentional ducking remains only for narrative body/accusation/ending stingers.
- Additive-mix verification: production build and focused ESLint pass. The mandated client ran after the change, and `output/additive-audio-mix/gameplay.png` was visually inspected. Targeted Web Audio graph instrumentation confirmed zero shared compressor nodes and an unchanged music gain of 0.36 at idle, while walking, and during thunder; no console/page errors occurred.
- Dialogue typewriter/blip follow-up: added root-level `text-blip.mp3` as a preloaded loop at 42% gain on the SFX bus. NPC answers now reveal character-by-character at a steady cadence, with shorter comma/semicolon/colon breaths, an em-dash pause, and a 340 ms stop between complete sentences. The blip plays only while characters are advancing, pauses at sentence boundaries, restarts from the beginning for each new answer, and stops on answer replacement or interview close.
- Dialogue interaction safeguards: question choices remain disabled until the current reply finishes printing, the return-to-topics action waits for completion, and the visible gold cursor plus `aria-live` region communicate active output without changing the stored full transcript.
- Verification: production build, focused ESLint, diff check, and sentence-segmentation/reassembly assertions pass; Vite bundles the 173 KB text-blip asset. The mandated browser client ran after the change and produced no browser error artifact, but its first click now reaches the newly added case-setup screen rather than gameplay. Subsequent dedicated Chromium launches were blocked by a macOS process-port permission failure, so the new mid-interview screenshot could not be captured in this pass.
- Dialogue skip follow-up: added a subtle `Enter ↵ reveal` control inside the reply card plus a capture-phase Enter shortcut active only while text is printing. Either path cancels pending typewriter timers, reveals the exact full answer, removes the cursor/hint, stops `text-blip.mp3`, and unlocks questions; Enter is prevented from activating a focused question during the skip.
- Skip verification: production build, focused ESLint, diff check, and the mandated browser regression client pass. The dedicated mid-interview browser scenario remains unavailable because additional Chromium processes hit the same macOS process-port restriction noted above.
- Separate volume-controls follow-up: replaced the legacy master-volume setting with independent `bgmVolume` and `sfxVolume` values. BGM controls only the score bus; SFX controls ambience (rain and clock) plus footsteps, thunder, dialogue blips, and event cues. Global mute still overrides both.
- Preserved the established default mix by moving the old 70% multiplier from the master into both buses. Existing saved `volume` values migrate into both new controls on load and the deprecated key is removed from subsequent saves. Narrative music ducking now scales to the selected BGM level and bus changes use short independent ramps.
- Volume verification: production build, focused ESLint, diff check, and the mandated browser client pass. The settings screen was visually inspected in `output/separate-volume-settings/shot-0.png`; both labeled sliders, live percentages, and the existing game-speed/dialogue settings fit the ornate panel cleanly with no browser error artifact.
- Dialogue pacing follow-up: increased typewriter speed by roughly 18-20% while preserving its relative rhythm. Standard characters now advance every 31 ms (from 38), spaces every 20 ms, clause punctuation pauses 85 ms, em dashes 120 ms, sentence breaks 280 ms, and initial response pickup 70 ms. Blip synchronization and Enter/click reveal behavior are unchanged.
- Pacing verification: production build, focused DialoguePanel ESLint, diff check, and the mandated browser regression client pass; the inspected artifact is `output/dialogue-speed-regression/shot-0.png` and no browser error artifact was produced.

Calmer NPC movement follow-up (current prompt): guests felt random and repeatedly darted between rooms.
- Reduced guest walking speed from 2.4 to 1.55 world units/second and lengthened decision/linger intervals.
- Replaced mostly random adjacent-room travel with archetype-specific room preferences, purposeful social seeking, two-room travel memory to prevent immediate backtracking, and a much stronger tendency to stay put when there is no reason to move.
- Guests prioritize people they have not spoken with; repeated conversations are less likely. Murderer hunt/escape behavior remains strategically exempt from ordinary pacing.
- Updated LLM movement instructions to require an explicit character, person, or information-based reason before changing rooms and to avoid aimless back-and-forth travel.
- Verification: production build, focused ESLint, and full-night simulation smoke test pass. The mandated live browser run advanced the case to 12:40 AM with stable idle behavior and no browser errors; `output/calm-npc-movement/shot-1.png` and matching state were inspected.

Single-pass investigation animation follow-up (current prompt): user reported that the detective's body-investigation animation cycles repeatedly.
- Root cause: the two investigation atlas poses were selected with a global-time modulo, deliberately alternating about four to five times during the 1.15-second action.
- Added an action start timestamp and mapped normalized action progress to pose 14 for the first half and pose 15 for the second half. Each investigation now advances through the two authored poses exactly once before returning to directional idle.
- Production build and focused ESLint pass. The mandated web-game client completed a live gameplay run with no browser errors (`output/investigation-once-runtime/`). A targeted deterministic action test sampled the detective atlas through the full 1.15-second sequence and recorded `14,14,14,15,15,15,15,2`: one forward transition from the first investigation pose to the second, then a return to directional idle with the action cleared. Metadata and the visually inspected post-action capture are in `output/investigation-once-sequence/`.

Detective stopped-facing follow-up (current prompt): user reported that releasing left or right makes the detective face the wrong way.
- Root cause: the detective atlas stores horizontal idle profiles in the opposite column order from the horizontal walking profiles. The renderer reused the walking direction column when returning to idle, flipping the visible pose.
- Added a detective-only idle-row mapping that swaps horizontal columns 1 and 3 while leaving walking, vertical idle, turning, NPC, and investigation frames unchanged.
- Production build and focused ESLint pass. The mandated client verified an actual walk-left/release sequence (`output/detective-facing-left/`) with no browser errors. A deterministic two-direction check then forced walk-left/stop and walk-right/stop separately: the detective settles on atlas frame 1 facing screen-left and frame 3 facing screen-right, with both screenshots visually inspected in `output/detective-facing-horizontal/` and no console/page errors.

Character name-tag spacing follow-up (current prompt): labels visually merged into character heads and hats.
- Raised the living-actor label anchor from 1.95 to 2.32 world units, placing the bottom of each name above the 2.2-unit sprite silhouette. Dead-body label positioning remains unchanged.
- Production build and focused ESLint pass. The mandated client completed two live runtime captures in `output/raised-name-tags/`; both were visually inspected. The detective label now has a clean visible gap above the fedora instead of intersecting it, remains comfortably associated with the character, and no browser error artifacts were produced.

Room floor texture follow-up (current prompt): replace buggy procedural floors with room-specific sprite textures and fix feet clipping through the floor.
- Generated a 3x3 source atlas and cropped nine room-specific top-down floor sprites under `public/assets/floors/`.
- Replaced the overlapping procedural floor-language mesh stacks with one textured floor plane per room, eliminating the primary coplanar/z-fighting source.
- Raised standing character sprite planes to their mathematically correct half-height plus clearance so their feet no longer intersect the floor surface.
- Production build and focused world ESLint pass. Mandated web-game captures in `output/floor-textures-dining/` and `output/floor-textures-library/` were visually inspected; they show stable single-surface floors, distinct neighboring room materials, grounded character feet, correct room traversal/state, and no browser error artifacts.
- Cellar crop correction: the initial `sips` offset exports silently retained the entire 1254px atlas for off-center cells. Re-exported all nine room assets with explicit pixel crop rectangles; every consumer texture is now exactly 418x418. Visually inspected `cellar.png` and the cellar floor as rendered in the neighboring-room view in `output/cellar-floor-crop-fix-final/`; it now shows only the intended dark stone tile.

Player/NPC pass-through follow-up (current prompt): user reported that NPC collision can trap the detective.
- Removed living-guest proximity from player movement acceptance. Player movement still checks room boundaries, walls, passages, and furniture through `canOccupy`, while the NPC simulation retains its own guest-to-guest collision avoidance.
- Production build and focused game ESLint pass. The mandated client traversed the Dining Hall doorway normally with no browser errors (`output/player-npc-pass-runtime/`). A forced worst-case test placed an idle NPC directly in the detective's narrow south-door path: the detective advanced from z=2 through the NPC at z=3.5 to z=5.3, while the NPC stayed fixed. A furniture regression check from the same starting point still blocked northward movement at z=2 against the dining table. Metadata and the visually inspected capture are in `output/player-npc-pass-forced/`; no console/page errors occurred.

Conversation layout follow-up (current prompt): move the interview UI to the bottom-right, enlarge it and its character bust, and prevent long answers from resizing the panel.
- Reworked the interview panel into a fixed-height, viewport-capped bottom-right window with an independently scrollable conversation/question region and a pinned action footer.
- Increased the dialogue panel width from 42rem to 48rem and the character bust from 6rem square to 10rem square.
- Production build and focused ESLint pass. The mandated web-game client completed without browser errors, and a targeted live interview capture was visually inspected at `output/conversation-layout-final/interview.png`. Desktop metrics confirm a 768x672 panel anchored 16px from the bottom-right, a 158px rendered bust, active `overflow-y: auto`, and zero console/page errors.

Conversation space follow-up (current prompt): remove the dead area below the selectable questions and give it to the conversation response.
- Made the portrait/header row consume the available panel height and expanded the bordered conversation response area to fill that space; the selectable questions now sit directly beneath it with a tighter gap.
- Kept independent overflow scrolling on the expanded response area for unusually long answers.
- Locked the conversation response box itself to a 25rem height so short, long, and loading answers cannot alter its dimensions.
- Production build and focused ESLint pass. Mandated runtime and targeted interview verification pass; `output/conversation-fixed-height-final/interview.png` was visually inspected and browser metrics confirm the response box remains exactly 400px tall with `overflow-y: auto` and zero console/page errors.
- Halved the fixed response height from 25rem/400px to 12.5rem/200px and reduced the fixed panel height proportionally from 42rem to 29.5rem so the reclaimed space does not become another empty gap.
- Reduced the response box by another 50px to 9.375rem/150px and proportionally reduced the panel height to 26.375rem.

LLM case-memory and response-format follow-up (current prompt): keep character prompts current within a run, reset them for each new case, and normalize inconsistent quote wrapping.
- LLM decision and interview requests now include authoritative current-run memory assembled from that NPC's conversations, learned rumors, discovered victims, and prior detective exchanges. It is derived only from the freshly reset simulation and transcript state, so no memory crosses case boundaries.
- Prompts now specify exact response contracts, including unquoted dialogue. Runtime normalization removes repeated straight, curly, or single quote wrappers (and accidental text fences) before UI/log code adds its one canonical pair of curly quotes.
- Interview parsing also unwraps models that put the required emotion tag inside their outer quotation marks, ensuring both the tag and redundant quotes are removed consistently.
- Verification: production build, focused ESLint, diff checks, and targeted parser assertions pass for plain, quoted, double-wrapped, and tag-inside-wrapper responses. The mandated live-game capture/state in `output/llm-memory-format-runtime/` was inspected and contains no browser error artifact.

Dynamic LLM interview follow-up (current prompt): allow contextual dialogue progression without displacing important case questions.
- In LLM mode, once a character has answered, the least-important fourth question becomes a follow-up derived from the latest answer. It prioritizes a named guest, then a named room, then a concise quoted claim, with a generic probing fallback.
- The three stronger phase-aware questions remain unchanged. Built-in mode retains all four authored questions, and failed LLM answers still have a built-in `follow_up` response path.
- Replaced the original local name/room heuristic after feedback that it repeatedly produced generic, non-progressing questions. A dedicated LLM request now receives the exact latest exchange, prior questions, and current case memory, and must target a concrete unresolved detail, contradiction, person, place, time, motive, relationship, or claim.
- The follow-up prompt explicitly forbids generic variants of “tell me more,” “what do you mean,” “why is that important,” and “what aren’t you telling me,” rejects repeated questions, and returns no replacement when there is no grounded path to new information.
- Verification: production build, focused ESLint, and diff checks pass. The mandated live-game smoke test completed without browser errors. A mocked two-request LLM test confirmed the second request received the strict follow-up prompt and converted a specific cufflink claim into `Why was Julian hiding a bloodstained cufflink beneath the piano at 12:20?`; the rendered panel was visually inspected in `output/llm-followup-prompt-targeted/`.

Investigation-thread overhaul (current prompt): replace random evidence association rolls with bounded, choice-driven conversation paths.
- Removed the per-click 30% evidence roll. Each guest now receives four persistent run-scoped topic threads; three map once to that guest's three hidden evidence associations and the fourth is informational.
- Root questions begin a focused thread. Each turn offers three unique-ID branches owned by the game: one advances, one stalls, and one closes the subject. Two productive choices resolve a thread and unlock its mapped association; two stalls or a close exhaust it without evidence. Progress persists when the interview is reopened and resets with a new case.
- Added a strict structured LLM thread-turn prompt/API. The game supplies opaque branch IDs, private effects, stage goals, history, and case memory; the model may write only the NPC response and grounded detective wording. It cannot grant evidence or alter progression. Malformed, generic, repeated, missing, reordered, or extra choices fall back safely to authored branches.
- Interview buttons now use stable choice IDs rather than topics, show only the active thread's branches, and provide a return-to-topics action after resolution/exhaustion. Resolved/exhausted roots are disabled and labeled, preventing repeat farming.
- Verification: production build, focused ESLint, diff checks, full-night simulation, and the mandated browser client pass. Deterministic browser state coverage followed two correct branches to reveal exactly one mapped association, exhausted a second thread through its close branch without revealing another, confirmed both statuses persist after reopening, and confirmed a new case clears all thread state (`output/investigation-threads-state/meta.json`). A mocked LLM turn produced three distinct grounded questions with opaque advance/stall/close IDs and no premature evidence; the live panel capture was visually inspected at `output/investigation-threads-llm/branch-choices.png` with zero browser errors.

Zero-victim dialogue guard follow-up (current prompt): prevent opening interviews from referring to a victim before any body is discovered.
- Thread prompts now state the exact known-victim list. With zero discovered bodies they explicitly forbid references to victims, murder, killing, death, corpses, or bodies and constrain choices to whereabouts, relationships, observations, and suspicious behavior.
- Runtime validation independently rejects any zero-victim LLM answer or choice containing those concepts, forcing the safe authored fallback even if the model ignores its prompt.
- Authored fallback questions also sanitize quoted details from character greetings when no victim is known, so a rejected model response cannot reintroduce premature death language indirectly.
- Verification: production build, focused ESLint, diff checks, and the mandated opening-state browser run pass. A targeted mock deliberately returned `Can you tell me about the victim yet?` at 0 bodies; validation rejected the entire model turn, `llmActive` remained false, and all three rendered fallback choices were victim/death-free. The inspected capture and metadata are in `output/zero-victim-guard-targeted/` with zero browser errors.

Authored built-in investigation threads (current prompt): give offline/built-in NPCs the same meaningful scripted thread and evidence-path system as LLM mode.
- Added `src/game/authoredDialogue.ts`, a story-writer-authored catalog for all ten established characters: three canonical evidence routes plus one informational route each. Every route contains a unique pre-death-safe root/opening and two complete stages of character-specific advance, stall, and close questions, responses, and portrait emotions (40 routes, 240 authored choices).
- Restored canonical profession/evidence associations instead of shuffling whole evidence profiles between archetypes. Names, murderer identity, locations, timing, behavior, and crime scenes remain randomized, while every scripted physical habit now sensibly matches its character.
- Added `dialogueResolver.ts` for typed route lookup, authored choice rendering, canonical association matching, and runtime catalog validation. Built-in mode and every LLM failure path now render the exact authored story beat and choices rather than generic questions or unrelated `sim.answerQuestion` lines.
- The existing game-owned state machine remains the sole progression authority: two authored advance choices resolve a route, close or two stalls exhaust it, and only a terminal advance on an evidence route unlocks that route's association. Informational routes resolve without evidence. Terminal authored responses remain in transcripts before the UI displays the subject cap.
- Added exhaustive `authored-dialogue-test.ts` validation for all ten archetypes, all 30 canonical evidence routes, ten informational routes, two-stage branch structure, exact advance/stall/close shape, unique/nonempty content, and forbidden pre-death language.
- Verification: production build, focused ESLint, diff checks, full-night simulation, and the mandated browser client pass. Browser automation completed all 30 evidence routes across all ten archetypes and revealed exactly the 30 mapped associations (`output/builtin-authored-all-routes/meta.json`). Separate close, double-stall, and informational paths resolved/exhausted with zero reveals (`output/builtin-authored-negative-paths/meta.json`). The live built-in Columnist opening and its three authored choices were visually inspected at `output/builtin-authored-visual/columnist-opening.png`; zero browser errors.

LLM conversation conclusion follow-up (current prompt): stop exhausted dialogue threads instead of allowing endless follow-up loops.
- Interview responses now carry a required `[continue]` or `[closed]` status after the emotion tag. Prompts tell the character to close when the subject is fully answered, further replies would repeat information, no relevant information remains, or the exchange has naturally concluded.
- A closed result preserves the substantive answer in the transcript, clears further question choices, resets the portrait to neutral, and displays exactly `nothing more to discuss right now` in the conversation window. Legacy/malformed responses default safely to continuing.
- Verification: production build, focused ESLint, diff checks, and parser assertions pass. A mocked live LLM returned `[thoughtful] [closed]`; the runtime cleared all four questions, selected neutral, and rendered the exact unquoted conclusion message with zero browser errors. The inspected capture and metadata are in `output/conversation-concluded-targeted/`.

Minimap sizing follow-up (current prompt): increase the minimap so its width matches the option row beneath it.
- Made the top-right HUD stack size to the button row and set the square minimap canvas to stretch to that shared width, with a higher 300x300 drawing buffer for crisp rendering at the larger size.
- Production build passed. The mandated web-game client entered live gameplay and the resulting capture was visually inspected at `output/minimap-width-gameplay/shot-0.png`; the minimap and four-button row have matching left/right edges and no browser error artifact is present.
- Increased room-label text from 8px to 12px to suit the enlarged minimap.
- Production build passed and the mandated live-game capture at `output/minimap-text-size/shot-0.png` was visually inspected; labels are clearer, remain centered within every room, and show no clipping or browser error artifact.

Evidence journal follow-up (current prompt): create a sprite for every evidence-bank trace and add an Evidence journal tab populated by collected clues.
- Generated and chroma-keyed ten matching 256x256 transparent pixel-art evidence sprites under `public/assets/evidence/`; the full set was visually reviewed in `output/evidence-sprites/contact-sheet.png`.
- Added stable collected-evidence records to game state/snapshots, reset them per case, and populate them during body investigation with clue ID, scene, time, description, and possible profession sources.
- Added an Evidence journal tab with an empty state, live count, sprite cards, scene metadata, descriptions, and source badges.
- Production build and focused ESLint pass. Mandated web-game runtime completed without browser errors. A targeted real investigation flow verified the empty tab, body examination, evidence state/snapshot, render-to-text output, sprite loading, count update, description, scene metadata, and all three possible-source badges. Desktop and mobile captures were visually inspected in `output/evidence-journal-final/`; metadata reports 256px loaded art and zero console/page errors.
- Guest evidence association follow-up: each guest journal card now lists the archetype's exact three possible evidence traces with their matching sprites and labels, driven directly from `EVIDENCE_BY_ARCHETYPE` so the display cannot drift from murder-scene selection logic.
- Guest association verification: production build, focused ESLint, and mandated runtime pass. Desktop/mobile journal QA in `output/guest-evidence-final/` confirms all ten archetypes, exactly 30 associations, three 256px-loaded sprites per card, readable responsive layouts, and zero console/page errors.
- Evidence association sizing follow-up: reduced guest-card evidence tiles to a fixed compact height with 32px sprites and a fixed two-line label area, preventing long evidence names from changing row height or breaking the three-column grid.
- Compact evidence verification: production build, focused ESLint, and mandated runtime pass. Desktop/mobile captures in `output/compact-evidence-cards/` were visually inspected; all 30 tiles are exactly 72px tall, every label stays inside its fixed area without horizontal or vertical overflow, and no browser errors occur.

NPC pass-through follow-up (current prompt): remove NPC-to-NPC collision so guests cannot become stuck on each other.
- Removed living-guest proximity from NPC movement acceptance. NPCs can now pass through one another while room boundaries, walls, passages, and furniture remain collision-aware through `canOccupy`.
- Verification: production build, focused ESLint, and full-night simulation pass. The mandated browser client advanced a live case through NPC movement and a room transition with no browser error artifact; `output/npc-pass-through-runtime/shot-0.png` and its matching text state were visually inspected.

Lateral walk-cycle follow-up (current prompt): all character models should alternate their legs while walking sideways.
- Superseded diagnosis: alternating opposing side-profile columns within one row and mirroring them preserved the same effective stride. The corrected mapping and current verification are recorded in “Side-walk leg animation follow-up” above.

LLM conversation preplanning follow-up (current prompt): let the character model author the complete interview graph once, then preserve every choice and outcome for the run.
- Replaced per-turn LLM dialogue generation with one conversation-planning request when a guest's first interview begins. The request supplies current run facts, known victims, other guests, and that character's three allowed associations, and requires four complete routes (three evidence routes plus one informational route), each with an opening and two advance/stall/close stages.
- The prompt now requires exactly one unwrapped JSON object with a fixed schema, immutable route/evidence IDs, exact route ordering, allowed topics/emotions, and no extra fields. Runtime validation rejects malformed structure, duplicate or generic choices, invalid IDs, premature death language, tags, and out-of-bounds text.
- Valid plans are stored per guest for the lifetime of the case. Reopening an interview reuses the same graph and retained resolved/exhausted state without another model request; starting a new case clears both plans and thread progress. Invalid JSON or a failed request freezes that guest onto the complete built-in authored plan for the rest of the run.
- The game remains progression authority: only the planned second ADVANCE unlocks its assigned association, while stalls and closes cannot reveal evidence. No later LLM call can rewrite choices or outcomes.
- Verification: production build, focused ESLint, and diff checks pass. A mocked live LLM made exactly one request, returned the full four-route JSON plan, completed a two-advance route to reveal only its mapped association, and reopened with the resolved route disabled and the exact plan intact. Prompt, state, and zero-error metadata are in `output/llm-conversation-plan-targeted/meta.json`; `planned-branches.png` was visually inspected.
Gothic window-border follow-up: added a shared antique-brass double frame with engraved corner flourishes, central diamond finials, inner linework, and a subtle gold inset glow to the interview chat, Case Journal, and Settings window.
- Verification: production build and focused ESLint pass. The mandated web-game client ran without browser errors. Settings and journal captures were visually inspected in `output/gothic-borders-settings/` and `output/gothic-borders-qa/`; all four corner ornaments and both finials render without clipping or blocking controls.
- Designer-sprite replacement follow-up: replaced the code-drawn filigree with two dedicated generated transparent frame sprites: a 768×422 wide chat frame and a 736×544 popup frame. The shared frame component now selects the correct asset variant, and chat/journal/settings content uses measured ornamental safe areas so the richer corner work does not block controls or copy.
- Designer-sprite verification: exact RGBA dimensions and transparent centers/corners were validated. Production build, focused ESLint, and the mandated game client pass. Settings, journal, and a live NPC interview were visually inspected in `output/gothic-sprite-layout-final/` and `output/gothic-chat-final/`; metadata reports zero browser errors.
- Journal frame capacity follow-up: enlarged the journal from 736×544 CSS pixels to a 928×688 target while retaining 94vw/94vh viewport caps. The overlay, frame, and scroll viewport now explicitly clip horizontal/external overflow; only the journal's inner content region scrolls vertically.
- Chat frame capacity follow-up: enlarged the interview panel from 768×422 to a 960×528 target at the same sprite aspect ratio, with viewport caps. The outer chat wrapper and frame clip all external/horizontal overflow while the inner conversation region alone retains vertical scrolling.
- Enlarged-window verification: production build, focused ESLint, and mandated runtime pass. Final 1440×900 journal and 1280×720 live-interview captures were visually inspected in `output/enlarged-windows-final-2/`. Measured frame sizes are exactly 928×688 and 960×528; each reports matching client/scroll dimensions, computed outer overflow `hidden`, and zero browser errors.
- Gothic HUD follow-up: carried the generated antique-brass artwork into the top-right HUD. The minimap uses the popup sprite as a sliced frame so its carved corners retain their proportions around a square map; the four controls sit in a separate narrow action frame sliced from the chat sprite. Map readability and button hit targets remain intact.
- Gothic HUD verification: production build, focused ESLint, and mandated runtime pass. Desktop and compact captures were visually inspected in `output/gothic-hud-final/`; interaction coverage confirms Journal, Accuse, Mute, and Pause all remain clickable, and both viewport runs report zero browser errors.
- Ornate-window containment follow-up: increased the ornamental safe-area padding in chat, journal, and settings. Removed their full rectangular background fills and replaced them with frame-inset dark surfaces, preventing the panel background from showing beyond transparent carved corners. All outer windows continue to clip overflow, while only their intended inner content regions scroll.
- Ornate-window containment verification: production build, focused ESLint, and mandated runtime pass. Settings, enlarged journal, and a live interview were visually inspected at 1440×900 in `output/window-containment-final/`; dark surfaces and content stay inside the carved rails, computed frame overflow is `hidden`, and metadata reports zero browser errors.
- Guest-list viewport containment follow-up: converted the journal body spacing from internal padding to actual outer margins around the scroll viewport. The guest list's clipping rectangle now ends before the side and bottom frame rails, so cards cannot paint beneath or beyond the ornament while scrolling.
- Guest-list ornament clearance refinement: increased the scroll viewport margins to the frame's true clear opening, accounting for the deep lower corner flourishes rather than only the thin outer rail. The full last card row now scrolls to completion above the ornament.
- Guest-list containment verification: production build, focused ESLint, and mandated runtime pass. Top and fully scrolled guest-list captures were visually inspected in `output/guest-list-containment-final-2/`; the 640×291 scroll viewport sits wholly inside the ornament opening, uses hidden horizontal/automatic vertical overflow, shows the complete final row above the lower flourishes, and reports zero browser errors.
- Remaining UI-frame follow-up: extended the generated Gothic brasswork to the title/start menu, top-left clock and room panel, and bottom-left in-game event feed. Each uses sliced designer artwork so corners retain their form while rails adapt to the surface dimensions; the existing framed Settings menu remains unchanged.
- Remaining UI-frame verification: focused Screens/HUD ESLint and the mandated runtime pass. The framed title was inspected at 1280×720; clock/feed gameplay was inspected at 1440×900 and 1024×720 in `output/remaining-ui-frames-final/`, with zero browser errors. Full TypeScript build is currently blocked by an unrelated in-progress `ceilingInsects` unused-field error in `src/game/world.ts`; that separate gameplay work was preserved untouched.
- How-to frame follow-up: routed the How to Play overlay through the same generated popup-frame treatment and ornamental safe-area padding as Settings.
- How-to frame containment correction: ornate overlays now keep the generated frame stationary and place long content in a separate inset scroll viewport. How to Play was inspected at its top and fully scrolled bottom in `output/howto-frame-final/`; all copy and the Back control remain above the lower crest, scrolling measures 368px client/554px content, targeted ESLint passes, and browser errors are empty. The unrelated `world.ts` build blocker remains untouched.
- Guest-minds frame follow-up: routed the pre-case “Choose the guests' minds” setup overlay through the same fixed generated popup frame and inset scroll viewport used by Settings and How to Play.
- Guest-minds frame verification: focused Screens ESLint and mandated runtime pass. Built-in, LLM top, and fully scrolled LLM configuration states were visually inspected in `output/guest-minds-frame-final/`; the 368px inner viewport contains 737px of configuration content, reaches its 369px scroll maximum with controls above the lower crest, and reports zero browser errors. The unrelated `world.ts` build blocker remains untouched.
- Ornate overlay aspect-ratio correction: locked Settings, How to Play, and guest-minds overlays to the popup sprite's native 736:544 ratio. Their width now resolves from the smallest of 58rem, 94vw, or the width allowed by 94vh, preventing vertical stretching and giving long instructional copy a wider reading measure.
- Aspect-ratio viewport refinement: included the overlay's 24px top/bottom padding in the height-derived width calculation, eliminating the last small ratio drift when the frame is viewport-height constrained.
- Deployed sliced-frame compatibility fix: production contained both PNG assets, but every missing deployed frame shared the CSS `border-image-source: var(...)` path. Replaced title, minimap, action rail, clock, and text-feed border images with ordinary `<img>` overlays through the same `GothicFrame` component already proven on deployed chat/journal/settings surfaces. Preserved equivalent safe-area padding and removed the fragile custom-property border-image dependency.
- Deployment-safe frame padding follow-up: increased safe-area padding for the title menu, minimap, action rail, clock/room panel, and event feed so their contents sit farther inside the generated ornaments after switching from CSS nine-slice borders to direct image overlays.
- Deployment-safe frame verification: production build and focused Screens/HUD ESLint pass. Built output now contains relative `./assets/ui/gothic-${variant}-frame.png` image sources and no `border-image-source: var(...)` dependency. Title and gameplay captures in `output/deployed-frame-fix-final/` show all five converted surfaces; runtime metadata confirms all four simultaneous HUD frame images loaded at 736×544 or 768×422 with zero browser errors.
- Title-frame padding refinement: widened the title frame target from 64rem to 72rem and increased its safe-area padding to 112px top, 128px sides, and 120px bottom, keeping the masthead single-line while moving all menu content farther from the ornaments.
- Title-padding verification: production build, focused Screens ESLint, and mandated runtime pass. The 1280×720 title capture in `output/title-padding-final/` was visually inspected; the masthead remains single-line and the subtitle, buttons, and control hint all sit farther inside the frame with zero browser errors.
- Menu-family padding follow-up: increased title safe-area padding again to 128px top, 160px sides, and 144px bottom while widening its target to 76rem and capping the masthead at 6xl. Increased Settings, How to Play, and guest-minds popup padding to 176px top, 112px sides, and 160px bottom; long content continues to scroll within the inset viewport.
- Menu-padding verification: production build, focused Screens ESLint, and mandated runtime pass. Title, Settings, and How to Play captures were visually inspected in `output/menu-padding-final/`; Settings and How to Play retain 336px inset viewports with scrollable content, and browser errors are empty.
- Chat/HUD overlap follow-up: reduced the interview window from 960×528 to 864×476 (about 10%) and reserves the 24rem right-side HUD column at xl desktop widths. Narrower layouts remain right-aligned and viewport-capped.
- Chat-size verification: production build, focused DialoguePanel ESLint, and mandated runtime pass. Desktop layout now places the chat's right edge at 896px and the HUD action rail's left edge at 912px in a 1280px viewport, preserving a 16px gap instead of overlap; live gameplay/HUD was visually inspected in `output/chat-size-client-final-2/`.
- Chat-padding follow-up: increased the conversation safe area from 12% to 13.5% horizontally, top clearance from 10% to 11.5%, and footer bottom clearance from 9% to 11.5%. The main conversation viewport retains automatic vertical scrolling and hidden horizontal overflow.
- Chat-padding verification: production build and focused DialoguePanel ESLint pass. The mandated runtime client completed twice with no browser-error artifact; the current 1280×720 HUD composition was visually inspected in `output/chat-padding-final/` and retains clear separation between the interview region and right-side controls.
- Chat-layout correction: restored the 768:422 source artwork's native aspect ratio after the interview panel had drifted to a vertically stretched 864×576 box. The width now scales from the 54rem desktop target against both viewport width and height, while retaining the increased 13.5% horizontal and 11.5% vertical safe-area padding.
- Chat-content-flow correction: live inspection revealed the flexible portrait/reply row shrinking beneath its fixed 150px reply box, allowing the question grid to overlap it. Made the top interview block non-shrinking so questions remain in normal flow and the already-inset conversation viewport scrolls when its contents exceed the available height.
- Chat-density refinement: reduced the independently scrollable reply box from 150px to 112px so the portrait, reply, and two-row question grid fit together inside the newly padded native-ratio frame at 1280×720 without requiring the main conversation viewport to start with choices below its fold.
- Chat-choice visibility refinement: the live 1280×720 capture showed the third choice correctly flowing beneath the first two but below the initial viewport fold. Reduced the independently scrollable reply area to 96px and arranged choices in one three-column row at `sm` and larger widths; narrow screens retain a stacked, scrollable layout.
- Bottom-left dialogue composition: removed the persistent HUD text feed and moved interviews into its bottom-left corner. Expanded the desktop interview panel to an 880×576 target while reserving the right-side HUD column, restored a roomier 112px reply area and two-column choice grid, and retained viewport caps plus inset scrolling for smaller screens.
- Bottom-right map controls: moved the minimap and its framed action rail from the top-right to the bottom-right. Added a Map/Hide button and `M` keyboard shortcut that collapse or restore the framed minimap while leaving the action rail accessible.
- Bottom-right map verification: production build and focused HUD ESLint pass. Mandated runtime captures in `output/map-bottom-expanded/` and `output/map-bottom-toggle-button/` were visually inspected; the button collapses the map while keeping the rail anchored at the lower-right. A targeted keyboard test confirmed `M` changes the map element count 1→0→1 with zero console or page errors.
## Antiquarian death-pose scale correction (2026-07-20)

- Normalized the antiquarian's v3 death frame from a 311 px-wide silhouette to 270 px while preserving its aspect ratio.
- Recentered the pose within its 320 px atlas cell and retained the shared 294 px ground baseline, so it matches the scale of his standing and walking poses.
- Verified all ten NPC v3 atlases with `scripts/validate_npc_v3_atlases.py`.
- Verified the production build with `npm run build` and loaded the game through the Playwright web-game workflow with no console or page errors.
- Visual QA artifacts: `output/antiquarian-death-scale/atlas-comparison.png` and `output/antiquarian-death-scale/live-death.png`.

## Greenhouse curator scale correction (2026-07-20)

- Added a structured per-archetype scale override to the v3 NPC atlas metadata and set the curator to 0.92, reducing all of her in-world poses uniformly by 8%.
- Kept the source sheet at the shared 270 px standing-height contract, so atlas layout, animation frames, and portrait sizing remain consistent with the other NPC sheets.
- Applied the same character multiplier to idle, walking, action, and death rendering; horizontal facing still preserves the correct flip sign.
- Verified all ten NPC atlases, production build, focused ESLint, and the mandated Playwright workflow. A targeted live capture confirmed `spriteScale: 0.92` with zero console/page errors and was visually inspected at `output/curator-scale/curator-live.png`.

## Debutante walk-scale pulse correction (2026-07-20)

- Found detached generation artifacts at the top of all four `walkA` cells. The normalization pass included those fragments in the 270 px height, making the actual character shorter only during that animation beat and creating an apparent scale pulse.
- Removed the detached fragments and renormalized the four affected figures to the shared 270 px standing height and 294 px foot baseline. Idle, passing, and `walkB` cells were left unchanged.
- Verified all ten NPC atlases, production build, focused ESLint, and the mandated Playwright workflow. Visually inspected the repaired side-facing sequence at `output/debutante-scale-pulse/frame-audit-fixed.png`.

## Columnist and accountant front-view width correction (2026-07-20)

- Reduced the horizontal width of the society columnist and estate accountant's front-facing idle, `walkA`, passing, and `walkB` figures by 8% while preserving their 270 px height, centered alignment, and 294 px foot baseline.
- Left rear, side, action/death, and portrait cells unchanged.
- Verified all ten NPC atlases, production build, focused ESLint, and the mandated Playwright workflow. The corrected front/rear comparison was visually inspected at `output/front-view-width-fix/after.png`.

## NPC feet-to-shadow registration correction (2026-07-20)

- Corrected the shared NPC render-plane height using the atlas contract: each 320 px cell has a 294 px foot baseline, leaving 26 transparent pixels below the visible feet. The NPC plane now compensates by exactly 0.1990625 world units.
- Applied the correction centrally to all v3 NPCs while leaving the detective's separately registered atlas unchanged. Per-character scale overrides, including the curator's 0.92 multiplier, continue to preserve ground contact.
- Verified production build, focused ESLint, all ten NPC atlases, and the mandated Playwright workflow. A representative live lineup was visually inspected at `output/npc-ground-contact/lineup.png`; runtime metadata reports the corrected plane height and zero browser errors.

## Debutante front-view width correction (2026-07-20)

- Reduced the debutante's front-facing idle, `walkA`, passing, and `walkB` figures by 8% horizontally while preserving their 270 px height, centered alignment, and 294 px foot baseline.
- Left rear, side, action/death, and portrait cells unchanged.
- Verified all ten NPC atlases, production build, focused ESLint, and the mandated Playwright workflow. The before/after comparison was visually inspected at `output/debutante-front-width/comparison.png`.

## Society columnist death-pose repair (2026-07-20)

- Removed an orphaned pair of standing legs from the top of the columnist's front action/death cell. A 123 px transparent gap separated that accidental fragment from the intact prone figure, producing the apparent two-piece body in game.
- Preserved the prone death artwork, its scale, and its atlas-cell registration unchanged.
- Verified all ten NPC atlases, production build, focused ESLint, and the mandated Playwright workflow. The repaired pose was visually inspected at `output/columnist-death-repair/after-close.png`.

## All-NPC death and chalk-outline normalization (2026-07-20)

- Audited both runtime action cells across all ten v3 NPC atlases. Seven sheets contained detached standing/walking/portrait fragments; valid death and outline footprints varied from roughly 260–320 px wide.
- Isolated the intended prone body in every death cell, removed all disconnected generation debris, centered each pose, and normalized every maximum footprint to 270 px.
- Rebuilt every chalk outline deterministically from its cleaned death silhouette. Each outline is now one continuous chalk ring with the exact same centered 270 px footprint as its corresponding body; this also replaced the surgeon's unusable portrait-filled outline cell.
- Preserved locomotion and portrait rows. Original atlas backups for this pass are stored under `output/npc-action-audit/originals/`.
- Verified all ten NPC atlases, production build, focused ESLint, the mandated Playwright workflow, and runtime selection of frames 16/17 at the shared 1.15 action scale with zero browser errors. The complete final contact sheet was visually inspected at `output/npc-action-audit/final.png`.

## Body-discovery sting scope (2026-07-20)

- Restricted body-related mystery-sting playback to the player's room-entry discovery path (`playerDiscovers`); opening-crime setup and NPC discoveries are silent. Per user clarification, interview evidence reveals continue to use the same sting.
- Verified the initial restriction with a production build, focused ESLint, `git diff --check`, and a mandated Playwright gameplay pass. `output/body-sting-regression/shot-0.png` and `state-0.json` were inspected with no browser-error artifact.

## Clock-tick audibility correction (2026-07-20)

- Raised only the authored `ticktock.mp3` channel from 0.10 to 0.28 gain. The clock remains on the SFX-controlled ambience bus and synchronized to simulated minutes, but now stays audible beneath the rain and music mix.
- Production build, focused audio ESLint, and diff check pass. The mandated browser workflow reached gameplay and its screenshot/state were inspected in `output/clock-audibility-regression/`. A targeted runtime probe confirmed the AudioContext is running and the unmuted clock element is loaded, playing at 1×, and advancing with no console errors.

## Lo-fi evidence-discovery cue (2026-07-20)

- Added root-level `discovery.mp3` as the dedicated evidence cue. It is processed at runtime through 120 Hz high-pass, 3.2 kHz low-pass, gentle compression, and restrained gain for a worn lo-fi tone.
- Evidence learned from interview dialogue and physical evidence collected while investigating a body both trigger this cue. The chord cue remains exclusive to player-discovered bodies.

## Evidence reveal animation (2026-07-20)

- Added a shared top-center animated evidence reveal for both interview associations and physical scene discoveries. The evidence sprite pops into view with expanding rings, a radial mint-and-gold particle burst, and a short card entrance/exit; reduced-motion users receive a static notification.
- Production build, focused ESLint, diff check, and the mandated browser client pass. Targeted association and physical-discovery probes confirmed 16 particles, two glow rings, correct context-specific copy and sprites, automatic 4.2-second dismissal, and zero browser errors. Final gameplay captures were visually inspected in `output/evidence-reveal-targeted/`.
- Production build, focused audio/game ESLint, and diff check pass. The mandated browser workflow reached live gameplay and `output/discovery-cue-regression/shot-0.png` plus state were inspected. A targeted runtime probe confirmed the 1.488-second cue loads and plays through a running AudioContext while the BGM bus remains unchanged, with no console errors.

## Accusation window guest-view parity (2026-07-20)

- Rebuilt the standalone accusation menu inside the same ornate popup window used by the Case Journal.
- Replaced placeholder color swatches with the established guest portraits and matched the Guests tab's two-column cards, typography, borders, spacing, and suspicion bars.
- Kept the two-step accusation safeguard and restyled its confirmation view inside the framed window with the selected guest portrait.
- Production build, focused ESLint, and diff check pass. The mandated web-game client ran successfully; targeted list and confirmation captures were visually inspected in `output/accuse-window-targeted/`, showing nine living suspect cards and zero console/page errors.
